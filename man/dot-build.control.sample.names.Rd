% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_control_sample_names.R
\name{.build.control.sample.names}
\alias{.build.control.sample.names}
\title{Build Unique Control Sample Names}
\usage{
.build.control.sample.names(fluorophore, control.type, marker)
}
\arguments{
\item{fluorophore}{Character vector of fluorophore names, one per control.}

\item{control.type}{Character vector of control types (\code{"cells"}/\code{"beads"}),
one per control.}

\item{marker}{Character vector of marker/antigen names, one per control.
May contain \code{NA}.}
}
\value{
A character vector the same length as \code{fluorophore}, guaranteed to
contain no duplicates.
}
\description{
Derives a unique \code{sample} identifier for every row of a control table,
allowing multiple single-stained controls to be supplied for the same
fluorophore. Rows whose \code{fluorophore} is not duplicated are returned
unchanged. For a duplicated fluorophore, uniqueness is resolved in stages,
each applied only to the rows still in conflict after the previous stage:
\enumerate{
\item \code{"fluorophore (control.type)"}
\item \code{"fluorophore (control.type) (marker)"}
\item \code{"fluorophore (control.type) (marker)_1"}, \code{"..._2"}, etc.
}
}
