% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_variability.R
\name{assess.variability}
\alias{assess.variability}
\title{Assess Within-Fluorophore Spectral Variability}
\usage{
assess.variability(variant.list, peak.height = 0.1)
}
\arguments{
\item{variant.list}{Named list of spectral variant matrices (one per
fluorophore), as returned in the \code{variants} element of
\code{get.spectral.variants()}. Row 1 of each matrix is treated as the
reference spectrum; rows 2+ are treated as variants.}

\item{peak.height}{Numeric in \verb{[0, 1]}, default \code{0.1}. Detectors where
the reference spectrum falls below this fraction of that fluorophore's
own peak value (\code{peak.height * max(reference.spectrum)}) are excluded
from the denoised variability metric (\code{Denoised_rSD}). Relative to the
peak rather than an absolute cutoff, so behavior is unchanged whether
\code{variant.list} is L-infinity or L2 normalized.}
}
\value{
A numeric matrix with one row per fluorophore and four columns:
\describe{
\item{\code{rSD}}{Sum, across detectors, of the robust SD (MAD) of the
variant spectra.}
\item{\code{Scaled}}{\code{rSD} weighted by the reference spectrum, summed
across detectors, to downweight low-signal channels.}
\item{\code{Sim_rSD}}{Robust SD of the pairwise cosine similarities among
all variants (including the reference).}
\item{\code{Denoised_rSD}}{As \code{rSD}, but restricted to detectors where the
reference spectrum exceeds \code{peak.height}.}
}
Returns \code{NULL} (with a message) if no fluorophore has usable
variability data.
}
\description{
Summarises how much a fluorophore's spectral variants (rows 2+ of its
variant matrix, as produced by \code{get.spectral.variants()}) disagree with
its reference spectrum (row 1) and with each other, returning several
complementary summary metrics per fluorophore.
}
