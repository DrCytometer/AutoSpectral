% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_ggplot_fast.R
\name{.save.ggplot.fast}
\alias{.save.ggplot.fast}
\title{Save a ggplot Directly to a Raster Device}
\usage{
.save.ggplot.fast(
  plot,
  filename,
  width,
  height,
  dpi = 300,
  device = ragg::agg_jpeg,
  ...
)
}
\arguments{
\item{plot}{A ggplot object.}

\item{filename}{File path to save to.}

\item{width}{Numeric, plot width in inches.}

\item{height}{Numeric, plot height in inches.}

\item{dpi}{Numeric, resolution in dots per inch. Default \code{300}, matching
\code{ggplot2::ggsave()}'s default.}

\item{device}{A ragg device-opening function, e.g. \code{ragg::agg_jpeg} or
\code{ragg::agg_png}. Default \code{ragg::agg_jpeg}.}

\item{...}{Additional arguments passed to \code{device()}, e.g. \code{method = "fast"} or \verb{quality = } for \code{ragg::agg_jpeg()}.}
}
\value{
Invisibly, \code{filename}. Called for its side effect of writing the
plot to disk.
}
\description{
Saves a ggplot object by opening the target device directly and printing
the plot, rather than going through \code{ggplot2::ggsave()}'s own device-
resolution and dimension-guessing logic. Produces the same output as
\code{ggsave(filename, plot, device = device, width = width, height = height, dpi = dpi, ...)} with \code{units = "in"}.
}
