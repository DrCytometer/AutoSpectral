% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_unmix.R
\name{compare.unmix}
\alias{compare.unmix}
\title{Compare Unmixing Quality Across Two Spectral References}
\usage{
compare.unmix(
  single.stained.fcs,
  unstained.fcs,
  fluorophore,
  spectra,
  ref.spectrum,
  test.spectrum,
  cytometer,
  ref.spectra = NULL,
  test.spectra = NULL,
  x.channel = NULL,
  y.channel = NULL,
  x.min = -1000,
  y.min = -1000,
  x.width.basis = -1000,
  y.width.basis = -1000,
  gate = TRUE,
  gate.bound = NULL,
  ref.label = "Cells",
  test.label = "Beads",
  biplot.width = 5,
  biplot.height = 5,
  title = paste(ref.label, "vs", test.label, fluorophore),
  plot.dir = "./figure_compare_unmix"
)
}
\arguments{
\item{single.stained.fcs}{Character string. Path to the single-stained FCS
file for the fluorophore of interest.}

\item{unstained.fcs}{Character string. Path to the matched unstained (negative
control) FCS file.}

\item{fluorophore}{Character string. Name of the target fluorophore (must
match a row name in \code{spectra}, or will be appended automatically).}

\item{spectra}{Numeric matrix of spectral references (fluorophores × detectors,
values normalised 0–1) used as the starting point for constructing
\code{ref.spectra} and \code{test.spectra} when those are not supplied directly.}

\item{ref.spectrum}{Named numeric vector or single-row matrix. The reference
spectrum for \code{fluorophore} used in the first unmixing.}

\item{test.spectrum}{Named numeric vector or single-row matrix. The test
spectrum for \code{fluorophore} used in the second unmixing.}

\item{cytometer}{Character string identifying the cytometer model, passed to
\code{\link[=get.autospectral.param]{get.autospectral.param()}}. Also determines the unmixing algorithm:
\code{"id7000"} uses WLS, all others use OLS.}

\item{ref.spectra}{Optional numeric matrix. A pre-built full spectral
reference matrix (fluorophores × detectors) for the reference condition.
When \code{NULL} (default), this is constructed from \code{spectra} and \code{ref.spectrum}.}

\item{test.spectra}{Optional numeric matrix. A pre-built full spectral
reference matrix for the test condition. When \code{NULL} (default), this is
constructed from \code{spectra} and \code{test.spectrum}.}

\item{x.channel}{Unused. Reserved for future use.}

\item{y.channel}{Unused. Reserved for future use.}

\item{x.min}{Numeric. Minimum x-axis value (in data units) passed to
\code{\link[=create.biplot]{create.biplot()}}. Default \code{-1000}.}

\item{y.min}{Numeric. Minimum y-axis value (in data units) passed to
\code{\link[=create.biplot]{create.biplot()}}. Default \code{-1000}.}

\item{x.width.basis}{Numeric. Width basis for the biexponential x-axis
transform passed to \code{\link[=create.biplot]{create.biplot()}}. Default \code{-1000}.}

\item{y.width.basis}{Numeric. Width basis for the biexponential y-axis
transform passed to \code{\link[=create.biplot]{create.biplot()}}. Default \code{-1000}.}

\item{gate}{Logical. Reserved for future gating control; currently the
scatter gate is always applied. Default \code{TRUE}.}

\item{gate.bound}{Optional list with elements \verb{$x} and \verb{$y} defining a
polygon gate boundary in FSC/SSC space. When \code{NULL} (default), the gate is
detected automatically via \code{\link[=do.gate]{do.gate()}}.}

\item{ref.label}{Character string. Display label for the reference condition
(e.g., \code{"Cells"}). Also passed as \code{control.type} to \code{\link[=do.gate]{do.gate()}}.
Default \code{"Cells"}.}

\item{test.label}{Character string. Display label for the test condition
(e.g., \code{"Beads"}). Default \code{"Beads"}.}

\item{biplot.width}{Numeric. Width of each individual biplot in inches.
The saved figure is twice this wide (two panels). Default \code{5}.}

\item{biplot.height}{Numeric. Height of the saved figure in inches.
Default \code{5}.}

\item{title}{Character string. Title displayed on the figure and used as
the JPEG filename stem. Default is constructed from \code{ref.label},
\code{test.label}, and \code{fluorophore}.}

\item{plot.dir}{Character string. Directory for saving the output JPEG.
Created automatically if absent. Default \code{"./figure_compare_unmix"}.}
}
\value{
A data frame with one row per fluorophore in the reference spectra
matrix and four columns:
\describe{
\item{\code{Fluorophore}}{Fluorophore name.}
\item{\code{Reference.SSI}}{SSI under the reference spectrum.}
\item{\code{Test.SSI}}{SSI under the test spectrum.}
\item{\code{Reference.Spill}}{Fractional spillover under the reference spectrum.}
\item{\code{Test.Spill}}{Fractional spillover under the test spectrum.}
}
The figure is saved to \code{plot.dir} and the combined plot is also printed to
the active graphics device.
}
\description{
A diagnostic utility for comparing unmixing performance when two different
spectral references are available for the same fluorophore — for example,
a cell-derived reference versus a bead-derived reference, or two different
lot measurements. Both references are used to unmix the same single-stained
FCS file and the resulting Secondary Stain Index (SSI) and secondary spillover
values are compared side-by-side.

The function:
\enumerate{
\item Reads the single-stained and unstained FCS files.
\item Applies a FSC/SSC scatter gate (auto-detected or user-supplied).
\item Unmixes both gated datasets with each spectral reference matrix using
OLS (or WLS for the ID7000).
\item Identifies the fluorophore channel with the largest absolute SSI
across both unmixings and uses it as the y-axis for biplot visualisation.
\item Annotates each biplot with the median ± rSD of the unstained
distribution and the SSI / spillover value.
\item Saves a side-by-side JPEG to \code{plot.dir} and returns a summary table.
}

\strong{Note:} This function calls \code{calculate.ssi()}, which must be available in
the package namespace.
}
