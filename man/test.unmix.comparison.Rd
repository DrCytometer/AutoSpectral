% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_unmix_comparison.R
\name{test.unmix.comparison}
\alias{test.unmix.comparison}
\title{Test Whether Each Unmixing Method Differs From a Reference}
\usage{
test.unmix.comparison(
  results,
  summary,
  reference.folder = "AutoSpectral",
  test.method = c("wilcox", "t.test"),
  p.adjust.method = "BH",
  min.pairs = 4,
  log.transform = FALSE,
  output.csv = "unmix_comparison_stats.csv",
  detail.csv = "unmix_comparison_stats_detail.csv",
  verbose = TRUE
)
}
\arguments{
\item{results}{Either the long-format \code{results} data frame returned by
\code{compare.unmix.folders()} (or read back in from its \code{output.csv}), or a
character path to that CSV.}

\item{summary}{Either the per-fluorophore \code{summary} data frame returned
by \code{compare.unmix.folders()} (or read back in from its \code{summary.csv}), or
a character path to that CSV.}

\item{reference.folder}{Character. Name of the folder every other folder
is compared against. Default \code{"AutoSpectral"}.}

\item{test.method}{Character, one of \code{"wilcox"} (paired Wilcoxon
signed-rank test, the default) or \code{"t.test"} (paired t-test). Wilcoxon is
the safer default given the typically small, non-normal, non-negative
per-fluorophore metrics involved.}

\item{p.adjust.method}{Character, passed to \code{stats::p.adjust()}. Default
\code{"BH"} (Benjamini-Hochberg). Must be one of \code{stats::p.adjust.methods}.}

\item{min.pairs}{Numeric, default \code{4}. Minimum number of fluorophores
present in both a folder and \code{reference.folder}, for a given metric,
before a test is attempted; below this, the comparison is recorded as
\code{NA} (with a message) rather than run on too few pairs to be meaningful.}

\item{log.transform}{Logical, default \code{FALSE}. Log10-transform \code{value}
before pairing and testing. This changes what is being tested: on the
raw scale the test asks whether the typical \emph{absolute difference}
between a folder and \code{reference.folder} is zero; on the log scale it
asks whether the typical \emph{ratio} is 1, which is often the more
appropriate question when a metric spans a wide range across
fluorophores. All values must be strictly positive when \code{TRUE} --
\code{stop()}s otherwise, rather than silently dropping observations from the
test.}

\item{output.csv}{Character. Path for the wide adjusted-p-value matrix
(folders in rows, metrics in columns). Default
\code{"unmix_comparison_stats.csv"}.}

\item{detail.csv}{Character. Path for the long-format detail table (one
row per folder/metric, with \code{n.pairs}, the test statistic, raw
\code{p.value}, and \code{p.adjusted}) — this is what explains \emph{why} a cell in
\code{output.csv} is \code{NA} (too few pairs vs. a failed test). Default
\code{"unmix_comparison_stats_detail.csv"}.}

\item{verbose}{Logical, default \code{TRUE}.}
}
\value{
Invisibly, a named list with \code{wide} (the folder x metric adjusted
p-value matrix also written to \code{output.csv}) and \code{detail} (the
long-format table also written to \code{detail.csv}).
}
\description{
For every metric produced by \code{compare.unmix.folders()}, runs one paired
test per folder against a chosen reference folder (typically
\code{"AutoSpectral"}), pairing observations by fluorophore. Six metrics are
tested: the five already in the per-fluorophore \code{summary} table (\code{SSI},
\code{Delta.MFI}, \code{Spillover.ratio}, \code{FPR}, \code{Mahalanobis}) plus
\code{Unstained.rSD}, pulled in from the long-format \code{results} table since it
is per-fluorophore but was never folded into \code{summary} (it has no
per-channel spread to summarize away).

P-values are adjusted once across every (folder, metric) test performed
in the call (\code{stats::p.adjust()}), not separately per metric or per
folder, since that is the full family of comparisons being made in one
call. This function only writes CSVs; it does not modify or add to the
\code{compare.unmix.folders()} figures.

See also \code{test.unmix.comparison.channels()}, which runs the same
procedure directly on \code{results} (one off-target channel at a time)
rather than on the fluorophore-level \code{summary}.
}
