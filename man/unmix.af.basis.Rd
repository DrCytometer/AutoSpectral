% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unmix_af_basis.R
\name{unmix.af.basis}
\alias{unmix.af.basis}
\title{Unmix With A Continuous Autofluorescence Basis}
\usage{
unmix.af.basis(
  raw.data,
  spectra,
  af.basis,
  af.spectra = NULL,
  return.fitted.af = FALSE,
  negative.tol = 0.05
)
}
\arguments{
\item{raw.data}{Expression data from raw FCS files. Cells in rows and
detectors in columns.}

\item{spectra}{Fluorophore spectral signatures, fluorophores in rows and
detectors in columns.}

\item{af.basis}{An AF basis from \code{get.af.basis}.}

\item{af.spectra}{Optional AF library used only to report a nearest-library
index for each cell, for compatibility with the discrete workflow. Default
\code{NULL}.}

\item{return.fitted.af}{Logical, default \code{FALSE}. Whether to return the
fitted autofluorescence in detector space.}

\item{negative.tol}{Numeric, default \code{0.05}. A cell is flagged when the most
negative detector of its reconstructed AF spectrum falls below
\code{-negative.tol} times its largest.}
}
\value{
A list with \code{fluorophores} (cells x fluorophores), \code{k} (cells x
components), \code{af} (total fitted AF scale per cell), \code{negative} (logical
vector) and, when requested, \code{fitted.af} and \code{af.index}.
}
\description{
Solves \code{y = t(spectra) \%*\% f + basis \%*\% k} for every cell, fitting
autofluorescence as a free combination of basis directions rather than
selecting one row from a discrete library. Because the basis is built from
the panel-oblique part of the AF library, the normal equations for \code{k} are
diagonal and the per-cell solve is a single projection.

Fits are unconstrained, so a cell whose reconstructed AF spectrum goes
meaningfully negative is flagged in \code{negative} and is a candidate for
falling back to the discrete library.
}
