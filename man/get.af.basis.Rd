% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconvolve_af_background.R
\name{get.af.basis}
\alias{get.af.basis}
\title{Get Autofluorescence Basis}
\usage{
get.af.basis(
  unstained,
  n.pc = "auto",
  max.pc = 6L,
  read.var = 125^2,
  n.permutations = 3L,
  max.events = 50000L,
  verbose = TRUE
)
}
\arguments{
\item{unstained}{Numeric matrix (events x detectors), raw unstained control,
already gated to the population of interest.}

\item{n.pc}{Integer or \code{"auto"}. Number of components to retain. \code{"auto"}
keeps every component whose singular value exceeds the largest value a
matrix of the same shape containing only read noise would produce.
Default \code{"auto"}.}

\item{max.pc}{Integer, hard cap on the number of components. Default \code{6}.}

\item{read.var}{Numeric, per-detector read noise variance in raw units, the
floor of the retention threshold when \code{n.pc = "auto"}. Default \code{125^2}.}

\item{n.permutations}{Integer, permutations used to build the retention
threshold when \code{n.pc = "auto"}. Each permutation costs one decomposition.
Default \code{3}.}

\item{max.events}{Integer, maximum events used for the decomposition.
Default \code{50000}.}

\item{verbose}{Logical, controls messaging. Default \code{TRUE}.}
}
\value{
Numeric matrix, components x detectors, with unit L2 rows and the
singular values attached as the \code{singular.values} attribute.
}
\description{
Builds a low-dimensional autofluorescence basis from an unstained control as
the leading right singular vectors of the raw, uncentred unstained matrix.
The leading component is therefore the mean background direction and the
remainder describe how background varies from cell to cell.

A single autofluorescence spectrum cannot represent that variation. In a real
sample autofluorescence is a family of profiles that tracks cell type, and
cell type also determines which markers are expressed, so whatever a single
row leaves behind is correlated with marker expression and is readily
mistaken for spillover by any method that fits one channel against another.
}
