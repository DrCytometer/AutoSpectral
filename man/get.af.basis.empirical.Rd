% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_af_basis_empirical.R
\name{get.af.basis.empirical}
\alias{get.af.basis.empirical}
\title{Build A Continuous Autofluorescence Basis From Raw Events}
\usage{
get.af.basis.empirical(
  unstained.exprs,
  spectra,
  n.components = NULL,
  var.explained = 0.99,
  trim.quantile = 0.99,
  max.events = 50000,
  seed = 1
)
}
\arguments{
\item{unstained.exprs}{Numeric matrix of raw unstained expression data,
cells in rows and detectors in columns. Columns must match \code{spectra}.}

\item{spectra}{Fluorophore spectral signatures, fluorophores in rows and
detectors in columns.}

\item{n.components}{Integer, number of basis directions to retain. Default
\code{NULL}, in which case the count is chosen from \code{var.explained}.}

\item{var.explained}{Numeric in (0, 1], default \code{0.99}. Fraction of the
out-of-span variance of the trimmed, subsampled event set the retained
basis must capture. Ignored when \code{n.components} is supplied.}

\item{trim.quantile}{Numeric in (0, 1], default \code{0.99}. Events whose
out-of-span residual norm exceeds this quantile are excluded before the
basis is built. Real unstained samples can carry debris, doublets, or
residual spillover-contaminated events; the SVD has no built-in robustness
to them, so they are trimmed explicitly rather than left to dominate the
leading components. Set to \code{1} to disable.}

\item{max.events}{Integer, default \code{5e4}. If more events remain after
trimming, a random subsample of this size is used to build the basis. The
basis is then applied to every event via \code{unmix.af.basis()}, so this only
bounds the cost of basis \emph{construction}, not of applying it.}

\item{seed}{Integer, default \code{1}. Seed for the subsampling step.}
}
\value{
A list with \code{basis} (detectors x components, real combinations of
the sampled events), \code{directions} (detectors x components, the orthonormal
out-of-span directions), \code{sigma} (singular values), \code{var.explained}
(cumulative fraction captured), \code{n.components}, and \code{n.trimmed} (events
excluded by \code{trim.quantile}). Compatible with \code{unmix.af.basis()}.
}
\description{
As \code{get.af.basis()}, but derives the basis directly from the panel-oblique
residual of real unstained single-cell events rather than from a SOM-derived
spectral library. A SOM library is itself a quantisation of the unstained
sample's cell-to-cell diversity into a fixed set of node shapes, so building
the basis one step further upstream, from the events the SOM was trained on,
removes that quantisation before it happens rather than fitting around it.

Structurally identical to \code{get.af.basis()}: both take the panel-oblique
residual of a set of reference spectra (there, SOM nodes; here, raw events),
take its SVD, and keep the leading out-of-span directions. Ranking by
out-of-span variance rather than total variance matters here more than in
the library case, because raw single-cell data also carries shot noise and
any residual spillover contamination, both of which add variance that has
nothing to do with autofluorescence shape.
}
\seealso{
\code{get.af.basis}, \code{unmix.af.basis}
}
