% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_mismatch_plot.R
\name{spectral.mismatch.plot}
\alias{spectral.mismatch.plot}
\title{Spectral Mismatch Plot}
\usage{
spectral.mismatch.plot(
  ref.spectrum,
  test.spectrum,
  ref.label = NULL,
  test.label = NULL,
  title = "Spectral comparison",
  bar.color = "lightblue",
  fluorophore.colors = c("black", "red"),
  plot.dir = NULL,
  line.width = 1,
  point.size = 1,
  color.palette = NULL,
  show.legend = TRUE,
  plot.width = NULL,
  plot.height = NULL
)
}
\arguments{
\item{ref.spectrum}{A named numeric vector (or single-row matrix) of
normalised spectral intensities for the reference spectrum. Names must
correspond to detector channel names.}

\item{test.spectrum}{A named numeric vector (or single-row matrix) of
normalised spectral intensities for the test spectrum. Must have the same
names as \code{ref.spectrum}.}

\item{ref.label}{Character string used as the legend label for the reference
spectrum. If \code{NULL} (default), the \code{rownames()} of \code{ref.spectrum} is used.}

\item{test.label}{Character string used as the legend label for the test
spectrum. If \code{NULL} (default), the \code{rownames()} of \code{test.spectrum} is used.}

\item{title}{Character string used as both the plot title and the JPEG
filename stem. Default is \code{"Spectral comparison"}.}

\item{bar.color}{Fill color for the spectral-difference bar chart.
Default is \code{"lightblue"}.}

\item{fluorophore.colors}{Character vector of length 2 giving the line colors
for the reference and test spectra respectively. Overrides \code{color.palette}
when non-\code{NULL}. Default is \code{c("black", "red")}.}

\item{plot.dir}{Directory to save the JPEG file. If \code{NULL} (default), the
current working directory is used. Created automatically if absent.}

\item{line.width}{Numeric. Line width for the spectral traces. Default \code{1}.}

\item{point.size}{Numeric. Point size for the spectral traces. Default \code{1}.}

\item{color.palette}{Optional viridis palette name (e.g., \code{"viridis"},
\code{"plasma"}) used when \code{fluorophore.colors} is \code{NULL}. Ignored when
\code{fluorophore.colors} is set. Default is \code{NULL}.}

\item{show.legend}{Logical. Whether to display the color legend on the
upper panel. Default is \code{TRUE}.}

\item{plot.width}{Numeric. Width of the saved figure in inches. If \code{NULL}
(default), an automatic width is derived from the number of detectors
(approximately 12 inches per 64 channels, minimum 3 inches).}

\item{plot.height}{Numeric. Height of the saved figure in inches. If \code{NULL}
(default), an automatic height is derived from the number of spectra rows.}
}
\value{
The combined cowplot object is returned invisibly. The figure is
always saved to disk as \verb{<plot.dir>/<title>.jpg}.
}
\description{
Produces a two-panel diagnostic figure comparing two fluorophore spectra. The
upper panel overlays the normalised spectral traces for a reference and a test
spectrum across all detectors. The lower panel shows the per-detector spectral
difference (reference minus test) as a bar chart, making it easy to identify
channels where the spectra diverge.

The combined figure is saved as a JPEG to \code{plot.dir} and returned invisibly as
a \code{ggplot} / cowplot object so it can be embedded in downstream reports or
further modified.
}
