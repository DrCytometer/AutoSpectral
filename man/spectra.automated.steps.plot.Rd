% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_spectra_automated_steps2.R
\name{spectra.automated.steps.plot}
\alias{spectra.automated.steps.plot}
\title{Plot Automated Spectra Extraction Pipeline Steps}
\usage{
spectra.automated.steps.plot(
  control.dir,
  control.def.file,
  asp,
  fluorophores = NULL,
  n.candidates = 1000L,
  n.spectral = 200L,
  k.neighbors = 2L,
  singlet.quantiles = c(0.85, 0.975),
  trace.colors = c(AF = "grey40", `Pre-orthogonalization` = "#1B9E77",
    `Post-orthogonalization` = "#D95F02"),
  cells.trace.color = "#D95F02",
  beads.trace.color = "#377EB8",
  af.trace.color = "grey40",
  singlet.retained.color = "black",
  singlet.excluded.color = "grey75",
  clean.positive.color = "red",
  clean.positive.point.size = NULL,
  brightest.fill.color = "steelblue",
  brightest.line.color = "black",
  brightest.panel.width = NULL,
  brightest.panel.height = NULL,
  unstained.point.color = "black",
  cosine.point.size = NULL,
  selected.size.mult = 5,
  max.points = 50000,
  panel.width = 4,
  panel.height = 4,
  composite.width = NULL,
  composite.height = NULL,
  output.dir = NULL,
  save = TRUE,
  file.type = "jpg",
  verbose = TRUE
)
}
\arguments{
\item{control.dir}{Character. Path to the directory containing the
single-stained control FCS files.}

\item{control.def.file}{Character. Path to (or filename of) the control
definition CSV, as used by \code{\link[=get.spectra.automated]{get.spectra.automated()}}. If the file has a
\code{control.type} column (as used by \code{\link[=define.flow.control]{define.flow.control()}} /
\code{\link[=check.control.file]{check.control.file()}}), a row with \code{control.type == "beads"} sharing a
fluorophore name with a "cells" row is treated as a paired bead control:
its \code{filename} is the positive bead sample, and its \code{universal.negative}
must point at the matching unstained bead file. When present, this pair
supplies the "\if{html}{\out{<Fluorophore>}} (Beads)" trace in panel F instead of the
static spectral reference library.}

\item{asp}{The AutoSpectral parameter list from \code{get.autospectral.param()}.}

\item{fluorophores}{Character vector of fluorophore name(s) (as they
appear in the \code{fluorophore} column of the control file) to illustrate.
Default \code{NULL} illustrates the first external-negative fluorophore found.
Fluorophores using internal-negative mode (no \code{universal.negative} entry)
are skipped with a warning, since orthogonalisation / cosine-filter / kNN
steps do not apply to them.}

\item{n.candidates}{Integer, default \code{1000}. Number of top-expressing
candidate events shown as the "brightest events" selection (panel C) and
carried into the cosine-filter biplot (panel D), matching the
\code{n.candidates} argument of \code{\link[=get.spectra.automated]{get.spectra.automated()}}.}

\item{n.spectral}{Integer, default \code{200}. Number of the above candidates
that would be retained after cosine filtering; these are drawn at full
opacity, the remainder at reduced opacity, matching
\code{\link[=get.spectra.automated]{get.spectra.automated()}}'s \code{n.spectral} argument.}

\item{k.neighbors}{Integer, default \code{2}. Number of nearest neighbours used
for the kNN scatter-match panel (panel E), matching
\code{\link[=get.spectra.automated]{get.spectra.automated()}}'s \code{k.neighbors} argument.}

\item{singlet.quantiles}{Numeric, default \code{c(0.85, 0.975)}. Quantile
thresholds for the two-stage FSC/SSC singlet discrimination shown in
panel A, matching \code{\link[=get.spectra.automated]{get.spectra.automated()}}.}

\item{trace.colors}{Named character vector of colours for the three traces
in panel B. Names must be \code{"AF"}, \code{"Pre-orthogonalization"} and
\code{"Post-orthogonalization"}.}

\item{cells.trace.color, beads.trace.color, af.trace.color}{Colours for the
three traces in panel F: the calculated per-cell profile ("Cells"), the
reference profile ("Beads"), and the matched-unstained AF trace.
Defaults \code{"#D95F02"} / \code{"#377EB8"} / \code{"grey40"}.}

\item{singlet.retained.color, singlet.excluded.color}{Colours for retained
and excluded events in panel A. Defaults \code{"black"} / \code{"grey75"}.}

\item{clean.positive.color}{Colour for the highlighted "clean positive"
events overlaid on panel A. Default \code{"red"}.}

\item{clean.positive.point.size}{Numeric or \code{NULL} (default). Point size
for the panel A highlight. If \code{NULL}, defaults to
\code{asp$figure.gate.point.size * 1.5}.}

\item{brightest.fill.color, brightest.line.color}{Fill and line colours for
the KDE-smoothed histogram in panel C. Defaults \code{"steelblue"} / \code{"black"}.}

\item{brightest.panel.width, brightest.panel.height}{Numeric or \code{NULL}
(default). Width/height (inches) of panel C. If \code{NULL}, defaults to
\code{panel.width * 2} / \code{panel.height}. When narrower than the overall
composite width, the panel is centred with blank padding on either side.}

\item{unstained.point.color}{Colour for unstained events in panel D.
Default \code{"black"}.}

\item{cosine.point.size}{Numeric or \code{NULL} (default). Point size for the
non-selected single-stained control events in panel D (these are often a
small, pre-selected "brightest events" pool, so the default point size
can be too small to see clearly). If \code{NULL}, defaults to
\code{asp$figure.gate.point.size * 1.3}.}

\item{selected.size.mult}{Numeric, default \code{5}. Events selected for the
final profile are drawn in panel D at \code{cosine.point.size} times this
multiple, in place of a gate polygon (nothing is actually excluded from
the plot -- the selection feeds forward as an average).}

\item{max.points}{Integer. Maximum events plotted per panel (randomly
downsampled beyond this for speed). Default \code{5e4}.}

\item{panel.width, panel.height}{Numeric. Width/height (inches) used per
sub-panel when sizing the saved composite figure. Defaults \code{4} and \code{4}.}

\item{composite.width, composite.height}{Numeric or \code{NULL} (default).
Override the overall saved figure dimensions (inches); if \code{NULL}, these
are computed from \code{panel.width} / \code{panel.height} (and
\code{brightest.panel.height}, if given).}

\item{output.dir}{Character or \code{NULL} (default). Directory to save the
composite figure(s) and the kNN scatter-match panel. Defaults to the
current working directory.}

\item{save}{Logical, default \code{TRUE}. Whether to save the composite figure
for each fluorophore to \code{output.dir}.}

\item{file.type}{Character string, one of \code{"jpg"} (default), \code{"tiff"},
\code{"png"}, or \code{"pdf"}.}

\item{verbose}{Logical, default \code{TRUE}. Print progress messages.}
}
\value{
Invisibly, a named list (one entry per fluorophore) each
containing the individual panel ggplot objects, the assembled
\code{composite} cowplot object, and the empirical peak / AF-collision
channels used.
}
\description{
Builds a manuscript-ready, multi-panel figure illustrating the internal
steps of \code{\link[=get.spectra.automated]{get.spectra.automated()}} for one or more single-stained control
samples: singlet gating (linear scale), AF-orthogonalisation peak-finding,
brightest-event candidate selection (KDE-smoothed histogram), the
cosine-similarity filter (biexponential biplot, continuous colour
gradient), and the kNN scatter-matched AF subtraction. Uses the same
building blocks as the rest of AutoSpectral (\code{\link[=create.biplot]{create.biplot()}}-style
biexponential biplots, \code{\link[=spectral.trace]{spectral.trace()}}, \code{\link[=scatter.match.plot]{scatter.match.plot()}}) so
panel styling matches the package's other figures.
}
\seealso{
\code{\link[=get.spectra.automated]{get.spectra.automated()}}, \code{\link[=create.biplot]{create.biplot()}}, \code{\link[=spectral.trace]{spectral.trace()}},
\code{\link[=scatter.match.plot]{scatter.match.plot()}}
}
