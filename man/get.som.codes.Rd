% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/som.R
\name{get.som.codes}
\alias{get.som.codes}
\title{Cluster Data with a Self-Organizing Map}
\usage{
get.som.codes(
  data,
  som.dim,
  rlen = 10L,
  radius = NULL,
  dist = 4L,
  seed = 1337L,
  threads = 0L,
  unit.norm = FALSE
)
}
\arguments{
\item{data}{Numeric matrix, training events x features. Must have colnames.}

\item{som.dim}{Integer, side length of the square SOM grid.}

\item{rlen}{Integer, number of epochs (full passes over the data).
Default \code{10}.}

\item{radius}{Length-2 numeric vector, start/end neighbourhood radius.
Default \code{NULL}, which derives both ends from the grid's own neighbour
distances (67th percentile down to a small non-zero floor). Only used
on the AutoSpectralRcpp path -- \code{FlowSOM::SOM()} manages its own radius
schedule internally.}

\item{dist}{Integer 1:4, distance function (1 manhattan, 2 euclidean,
3 chebyshev, 4 cosine). Default \code{4}.}

\item{seed}{Integer, RNG seed for the initial codebook sample. Callers
should pass \code{asp$bird.seed}.}

\item{threads}{Integer, OpenMP threads for the accelerated path. Default
\code{0} (all available cores). Ignored on the \code{FlowSOM::SOM()} fallback
path, which is single-threaded.}

\item{unit.norm}{Logical, default \code{FALSE}. Scale every training row to unit
L2 length before training. Appropriate with \code{dist = 4} (cosine), where the
distortion-minimising code is the mean of unit event vectors rather than
the mean of raw ones; without it, bright events dominate each node's code.
Leaves cosine distances, and therefore node assignments, unchanged. Has no
principled justification under \code{dist = 2} and is left off by default so
existing callers are unaffected. Note that the returned codes are then on
the unit-normalised scale.}
}
\value{
A list with \code{codes} (matrix, SOM nodes x features), \code{grid}, and
\code{nNodes}, matching the subset of \code{FlowSOM::SOM()}'s return value
actually consumed elsewhere in AutoSpectral (\code{map$codes}).
}
\description{
Replaces \code{FlowSOM::SOM()} / \code{EmbedSOM::SOM()} as the SOM clustering engine
behind \code{get.af.spectra()} and \code{get.fluor.variants()}. Uses the OpenMP-
accelerated batch SOM (\code{som_train_batch_cpp()}) from AutoSpectralRcpp when
available, falling back to \code{FlowSOM::SOM()} (pure R, no compiled
dependency) otherwise -- so a plain \code{AutoSpectral} install with no
\code{AutoSpectralRcpp} still works, just without the OpenMP speedup.
}
\keyword{internal}
