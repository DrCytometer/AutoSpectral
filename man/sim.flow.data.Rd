% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_flow_data.R
\name{sim.flow.data}
\alias{sim.flow.data}
\title{Simulate Spectral Flow Cytometry Data}
\usage{
sim.flow.data(
  spectra,
  asp,
  n.cells = 10000L,
  complexity = 1/3,
  layer.centroids = c(0.5, 0.05, 0.005),
  layer.cv = 0.2,
  af.spectra = NULL,
  af.scale.range = c(0.001, 0.05),
  af.scatter.slope = 0.1,
  variants = NULL,
  scatter.data = NULL,
  fsc.mean.log = log(2e+05),
  fsc.sd.log = 0.4,
  ssc.mean.log = log(80000),
  ssc.sd.log = 0.5,
  fsc.ssc.cor = 0.6,
  shot.noise = TRUE,
  counts.per.unit = 0.5,
  spillover.noise = TRUE,
  detector.noise = TRUE,
  af.variation = TRUE,
  spectral.variation = TRUE,
  seed = 42L
)
}
\arguments{
\item{spectra}{Numeric matrix of fluorophore spectral signatures, L-infinity
normalised, fluorophores in rows and detectors in columns. Required.
Defines both the panel and the detector layout.}

\item{asp}{The AutoSpectral parameter list from \code{get.autospectral.param}.
Used to resolve \code{expr.data.max} and cytometer-specific detector noise
presets.}

\item{n.cells}{Integer. Number of synthetic cells to generate. Default
\code{10000}.}

\item{complexity}{Numeric in (0, 1]. Fraction of fluorophores that are "on"
(non-zero) per cell, drawn without replacement. Default \code{1/3}.}

\item{layer.centroids}{Numeric vector of length 3, giving the high, medium,
and low expression centroids as fractions of \code{expr.data.max}. Default
\code{c(0.5, 0.05, 0.005)}.}

\item{layer.cv}{Numeric scalar. Coefficient of variation (on the log scale)
for expression within each layer. Default \code{0.2}.}

\item{af.spectra}{Numeric matrix of autofluorescence spectra, L-infinity
normalised, AF components in rows and detectors in columns. As returned by
\code{get.af.spectra}. If \code{NULL} (default), no AF is added.}

\item{af.scale.range}{Numeric vector of length 2. Range of the uniform
distribution from which per-cell AF abundance is drawn, as a fraction of
\code{expr.data.max}. Default \code{c(0.001, 0.05)}.}

\item{af.scatter.slope}{Numeric scalar. Controls how much SSC drives AF
abundance. AF abundance is multiplied by
\code{1 + af.scatter.slope * (ssc - median(ssc)) / mad(ssc)}. Default \code{0.1}.}

\item{variants}{List of spectral variant matrices as returned by
\code{get.spectral.variants} or \code{get.fluor.variants}. Each element is named by
fluorophore and contains a matrix of variant spectra (variants in rows,
detectors in columns). If \code{NULL} (default), the base \code{spectra} rows are
used for all cells.}

\item{scatter.data}{Optional numeric matrix of real scatter data (cells in
rows, at least two columns for FSC and SSC). When provided, rows are
sampled with replacement to generate synthetic scatter, bypassing the
log-normal model. Default \code{NULL}.}

\item{fsc.mean.log}{Numeric. Mean of the log-normal FSC distribution (on the
natural-log scale). Default \code{log(200000)}.}

\item{fsc.sd.log}{Numeric. SD of the log-normal FSC distribution. Default
\code{0.4}.}

\item{ssc.mean.log}{Numeric. Mean of the log-normal SSC distribution.
Default \code{log(80000)}.}

\item{ssc.sd.log}{Numeric. SD of the log-normal SSC distribution. Default
\code{0.5}.}

\item{fsc.ssc.cor}{Numeric in (-1, 1). Pearson correlation between log(FSC)
and log(SSC) in the synthetic scatter population. Default \code{0.6}.}

\item{shot.noise}{Logical. Whether to apply Poisson shot noise. Default
\code{TRUE}.}

\item{counts.per.unit}{Numeric scalar for \code{shot.noise}. Default \code{0.5}. Works
inversely; higher numbers will decrease shot noise-driven spread.}

\item{spillover.noise}{Logical. Whether to apply binomial spillover sampling.
Default \code{TRUE}.}

\item{detector.noise}{Logical. Whether to apply detector readout noise
(Gaussian, cytometer-specific). Default \code{TRUE}.}

\item{af.variation}{Logical. Whether to include autofluorescence variation
(requires \code{af.spectra}). Default \code{TRUE}.}

\item{spectral.variation}{Logical. Whether to sample per-cell spectral
variants (requires \code{variants}). Default \code{TRUE}.}

\item{seed}{Integer random seed for reproducibility. Default \code{42}.}
}
\value{
A named list with elements:
\describe{
\item{\code{raw}}{Numeric matrix (cells x detectors) of simulated detector-space
data, including all enabled noise sources.}
\item{\code{unmixed.no.af}}{Numeric matrix (cells x fluorophores) of OLS-unmixed
data without AF, for immediate inspection.}
\item{\code{truth}}{Named list:
\describe{
\item{\code{abundances}}{Matrix (cells x fluorophores+AF), true abundance per
cell. AF column appended as the last column when \code{af.spectra} is
provided.}
\item{\code{expression.layer}}{Character matrix (cells x fluorophores),
\code{"high"}, \code{"mid"}, \code{"low"}, or \code{NA} (off).}
\item{\code{variant.index}}{Integer matrix (cells x fluorophores), index of
the variant spectrum used per cell per fluorophore. \code{NA} when off or
no variants provided.}
\item{\code{af.row}}{Integer vector (length cells), which row of \code{af.spectra}
was assigned to each cell. \code{NA} when no AF.}
\item{\code{af.abundance}}{Numeric vector (length cells), true AF abundance
per cell in instrument units. \code{0} when no AF.}
\item{\code{scatter}}{Numeric matrix (cells x 2, columns FSC and SSC),
synthetic or resampled scatter values.}
}
}
\item{\code{params}}{Named list of all simulation settings used, for
reproducibility.}
}
}
\description{
Generates synthetic spectral flow cytometry data for development and
validation of AutoSpectral pipelines. Cells are assigned random fluorophore
expression levels drawn from three discrete expression layers (high, medium,
low), mixed into detector space via provided spectra (and optionally spectral
variants), and corrupted by configurable noise sources: binomial spillover
sampling, Poisson shot noise, and detector readout noise. Autofluorescence
variation is supported via a provided \code{af.spectra} matrix.

Ground-truth abundances, per-cell variant assignments, per-cell AF spectra,
and synthetic scatter are returned alongside the raw detector-space matrix
and a simple OLS unmixed result (no AF) for immediate inspection.
}
