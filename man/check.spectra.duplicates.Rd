% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_spectra_duplicates.R
\name{check.spectra.duplicates}
\alias{check.spectra.duplicates}
\title{Check Spectra for Duplicate Fluorophores}
\usage{
check.spectra.duplicates(spectra, collinearity.threshold = 0.95)
}
\arguments{
\item{spectra}{A matrix of spectral reference data (fluorophores x
detectors), as used by \code{unmix.fcs()} and related functions.}

\item{collinearity.threshold}{Numeric, default \code{0.95}. Cosine similarity
above which two rows trigger the fallback warning, used only when
\code{spectra} has no \code{"fluorophore"} attribute. Matches the package default
for \code{asp$similarity.warning.n}.}
}
\value{
Invisibly returns \code{TRUE} if no duplicates (or, in fallback mode, no
high-similarity pairs) are found. Stops with an informative error when an
exact duplicate is found via the attribute.
}
\description{
Verifies that a spectral reference matrix intended for unmixing contains
exactly one row per fluorophore. Multiple controls per fluorophore are
permitted upstream (during control file validation, spectrum extraction,
and QC comparison) via \code{sample}-based disambiguation, but two or more rows
representing the \emph{same} fluorophore must never reach the unmixing solvers:
near-identical reference columns are highly collinear and will badly
degrade unmixing conditioning. This function is the single enforcement
point that catches that before any unmixing math runs, and is called
automatically from \code{unmix.fcs()}, \code{unmix.folder()}, \code{unmix.autospectral()},
\code{unmix.autospectral.joint()}, \code{unmix.autospectral.rcpp()}, \code{unmix.gls()}
and \code{unmix.af.gls()}.

If \code{spectra} carries a \code{"fluorophore"} attribute (set automatically by
\code{get.fluorophore.spectra()} and \code{get.spectra.automated()}), true
fluorophore identity per row is checked exactly, and a duplicate stops
execution. If that attribute is absent -- notably, after \code{spectra} has
been round-tripped through a CSV via \code{read.spectra()}, which cannot carry
the attribute, and where a \code{sample}-disambiguated rowname is unique by
construction and so cannot reveal a duplicate either -- this falls back to
a pairwise cosine-similarity screen as a cheap, imperfect surrogate. That
screen cannot distinguish "two controls for the same fluorophore" from
"two genuinely similar fluorophores", so it warns rather than stopping.
}
