% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct_spectra_mean_delta.R
\name{correct.spectra.mean.delta}
\alias{correct.spectra.mean.delta}
\title{Correct Reference Spectra for Systematic Variant Offset}
\usage{
correct.spectra.mean.delta(spectra, variant.basis, fluorophores = NULL)
}
\arguments{
\item{spectra}{Numeric matrix (fluorophores x detectors).}

\item{variant.basis}{List from \code{build.variant.basis()}.}

\item{fluorophores}{Optional character vector restricting the correction
to named rows of \code{spectra}. Default \code{NULL} (all rows present in both
\code{spectra} and \code{variant.basis}).}
}
\value{
A list with \code{spectra} (corrected, L-infinity re-normalised) and
\code{variant.basis} (mean.delta zeroed for corrected fluorophores).
}
\description{
Shifts each fluorophore's reference spectrum by the mean delta captured
in \code{variant.basis} (see \code{build.variant.basis()}), then re-normalises to
L-infinity = 1. A non-zero mean delta means the control-derived
reference is systematically off-centre relative to the variant
population -- a bias in \code{spectra}, not a source of variance -- so
folding it into the covariance would inflate uncertainty while leaving
the bias itself uncorrected. This applies the correction once, up
front, so \code{unmix.gls()} sees a centred reference and \code{variant.basis}
describes only the residual spread around it.

The \code{mean.delta} entries in the returned \code{variant.basis} are zeroed for
every fluorophore corrected, since the mean is now baked into \code{spectra};
calling this function again on its own output is therefore a no-op.
}
