% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_gate.R
\name{apply.gate}
\alias{apply.gate}
\title{Apply Gate}
\usage{
apply.gate(
  flow.data,
  gate.boundary,
  scatter.param = asp$default.scatter.parameter,
  asp = NULL,
  min.fraction = 0.01
)
}
\arguments{
\item{flow.data}{A matrix or data frame of flow cytometry data (for
example, unmixed or raw expression data) with named columns, including the
two scatter parameters named in \code{scatter.param}.}

\item{gate.boundary}{A gate boundary, as returned by
\code{define.gate.landmarks()}, \code{define.gate.density()}, or \code{do.gate()} — a list
containing at least numeric \code{x} and \code{y} components describing the polygon
vertices.}

\item{scatter.param}{Character vector of length 2 giving the names of the
two scatter columns in \code{flow.data} to gate on. Default is
\code{asp$default.scatter.parameter}.}

\item{asp}{The AutoSpectral parameter list, prepared using
\code{get.autospectral.param()}. Only used to supply the default for
\code{scatter.param}; not required if \code{scatter.param} is supplied directly.}

\item{min.fraction}{Numeric between \code{0} and \code{1}, default \code{0.01}. If the
fraction of events retained by the gate falls below this value, a warning
is issued (the function still returns the gated data). Set to \code{0} to
disable this check.}
}
\value{
\code{flow.data}, subset to only those events falling inside
\code{gate.boundary}.
}
\description{
Applies a previously-defined scatter gate boundary to a set of flow
cytometry expression data and returns only the events falling inside it.
This is a lightweight, standalone counterpart to the gating step used
internally by \code{get.gated.flow.expression.data()}, intended for interactive
use and for testing other \code{AutoSpectral} functions against a gated subset
of data without going through the full control-file/FCS-reading pipeline.
}
\seealso{
\itemize{
\item \code{\link[=define.gate.landmarks]{define.gate.landmarks()}}
\item \code{\link[=define.gate.density]{define.gate.density()}}
\item \code{\link[=do.gate]{do.gate()}}
\item \code{\link[=check.gates]{check.gates()}}
\item \code{\link[=get.gated.flow.expression.data]{get.gated.flow.expression.data()}}
}
}
