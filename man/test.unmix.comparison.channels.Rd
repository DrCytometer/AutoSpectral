% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_unmix_comparison.R
\name{test.unmix.comparison.channels}
\alias{test.unmix.comparison.channels}
\title{Test Whether Each Unmixing Method Differs From a Reference, Per
Off-Target Channel}
\usage{
test.unmix.comparison.channels(
  results,
  reference.folder = "AutoSpectral",
  test.method = c("wilcox", "t.test"),
  p.adjust.method = "BH",
  min.pairs = 4,
  log.transform = FALSE,
  output.csv = "unmix_comparison_stats_channels.csv",
  detail.csv = "unmix_comparison_stats_channels_detail.csv",
  verbose = TRUE
)
}
\arguments{
\item{results}{Either the long-format \code{results} data frame returned by
\code{compare.unmix.folders()} (or read back in from its \code{output.csv}), or a
character path to that CSV.}

\item{reference.folder}{Character. Name of the folder every other folder
is compared against. Default \code{"AutoSpectral"}.}

\item{test.method}{Character, one of \code{"wilcox"} (paired Wilcoxon
signed-rank test, the default) or \code{"t.test"} (paired t-test).}

\item{p.adjust.method}{Character, passed to \code{stats::p.adjust()}. Default
\code{"BH"} (Benjamini-Hochberg). Must be one of \code{stats::p.adjust.methods}.}

\item{min.pairs}{Numeric, default \code{4}. Minimum number of on-target/
off-target channel pairs present in both a folder and
\code{reference.folder}, for a given metric, before a test is attempted;
below this, the comparison is recorded as \code{NA} (with a message).}

\item{log.transform}{Logical, default \code{FALSE}. Log10-transform \code{value}
before pairing and testing -- see \code{test.unmix.comparison()} for what
this changes about the hypothesis being tested. All values must be
strictly positive when \code{TRUE} -- \code{stop()}s otherwise.}

\item{output.csv}{Character. Path for the wide adjusted-p-value matrix
(folders in rows, metrics in columns). Default
\code{"unmix_comparison_stats_channels.csv"}.}

\item{detail.csv}{Character. Path for the long-format detail table (one
row per folder/metric, with \code{n.pairs}, the test statistic, raw
\code{p.value}, and \code{p.adjusted}). Default
\code{"unmix_comparison_stats_channels_detail.csv"}.}

\item{verbose}{Logical, default \code{TRUE}.}
}
\value{
Invisibly, a named list with \code{wide} (the folder x metric adjusted
p-value matrix also written to \code{output.csv}) and \code{detail} (the
long-format table also written to \code{detail.csv}).
}
\description{
The channel-level counterpart to \code{test.unmix.comparison()}: runs the same
paired-test-per-folder procedure, but directly on the full \code{results}
table rather than the per-fluorophore \code{summary} table, pairing
observations by the combination of on-target fluorophore and off-target
fluorophore (i.e. by spectral channel pair) rather than by fluorophore
alone. This uses every individual off-target-channel measurement instead
of first collapsing each fluorophore's off-target channels down to a
single rSD (MAD) value.

Only applies to the four metrics that have both an on-target and an
off-target fluorophore in \code{results} (\code{SSI}, \code{Delta.MFI},
\code{Spillover.ratio}, \code{FPR}); \code{Unstained.rSD} has no on-target fluorophore
(there is only one measurement per fluorophore channel, with no further
pairing dimension available) and \code{Mahalanobis} has no off-target
fluorophore (it is already a single summary value per fluorophore), so
both are excluded automatically by requiring both columns to be
non-missing.

\strong{Caveat:} the paired observations at this grain are not fully
independent -- every off-target channel belonging to the same on-target
fluorophore is measured on that fluorophore's single set of positive/
negative events, so within-fluorophore correlation inflates the
effective sample size the test sees. This will tend to produce smaller,
more "significant" p-values than a genuinely independent sample of the
same size would justify. Treat this as a more sensitive, exploratory
companion to \code{test.unmix.comparison()} (which pairs by fluorophore and
is the statistically conservative version), not a replacement for it.
}
