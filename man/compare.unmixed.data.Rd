% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_unmixed_data.R
\name{compare.unmixed.data}
\alias{compare.unmixed.data}
\title{Compare Two or More Sets of Already-Unmixed Data}
\usage{
compare.unmixed.data(
  data.list,
  channels,
  asp,
  gate.boundary = NULL,
  scatter.param = asp$default.scatter.parameter,
  variants = NULL,
  spread.kappa = 2,
  x.min = -5000,
  x.max = asp$expr.data.max,
  y.min = -5000,
  y.max = asp$expr.data.max,
  x.width.basis = -1000,
  y.width.basis = -1000,
  max.points = 5e+05,
  color.palette = "rainbow",
  panel.width = 3,
  row.height = 3,
  label.width = 1,
  label.size = 12,
  label.angle = -90,
  save = TRUE,
  title = "unmix_comparison",
  output.dir = NULL
)
}
\arguments{
\item{data.list}{A list of two or more matrices or data frames of
unmixed flow cytometry data, one per data set to compare. Every element
must have named columns. Column names do not need to match exactly
across data sets -- see \code{channels} below.}

\item{channels}{Character vector of channel names to plot, in the order
they should be paired (see Description). Must resolve to at least 2
channels present in every element of \code{data.list} after the intersect
described below.}

\item{asp}{The AutoSpectral parameter list, prepared using
\code{get.autospectral.param()}. Passed through to \code{\link[=create.biplot]{create.biplot()}} for every
panel, and supplies the default for \code{scatter.param}.}

\item{gate.boundary}{Optional gate boundary, as returned by
\code{define.gate.landmarks()}, \code{define.gate.density()}, or \code{do.gate()} -- a
list containing at least numeric \code{x} and \code{y} components describing the
polygon vertices. When supplied, every data set is gated with
\code{\link[=apply.gate]{apply.gate()}} before plotting, provided all data sets carry
\code{scatter.param}. Default \code{NULL} (no gating).}

\item{scatter.param}{Character vector of length 2 giving the names of the
two scatter columns to gate on, passed to \code{\link[=apply.gate]{apply.gate()}}. Only used when
\code{gate.boundary} is supplied. Default \code{asp$default.scatter.parameter}.}

\item{variants}{The variant list returned by \code{get.spectral.variants()},
passed through to every \code{\link[=create.biplot]{create.biplot()}} call for reference curves.
Default \code{NULL} (no reference curves).}

\item{spread.kappa}{Numeric, passed through to \code{\link[=create.biplot]{create.biplot()}}. Default \code{2}.}

\item{x.min, y.min}{Numeric, axis minima (data units) passed through to
\code{\link[=create.biplot]{create.biplot()}}. Default \code{-5000} for both.}

\item{x.max, y.max}{Numeric, axis maxima (data units) passed through to
\code{\link[=create.biplot]{create.biplot()}}. Default \code{asp$expr.data.max} for both.}

\item{x.width.basis, y.width.basis}{Numeric, biexponential width bases
passed through to \code{\link[=create.biplot]{create.biplot()}}. Default \code{-1000} for both.}

\item{max.points}{Numeric, per-panel point cap passed through to
\code{\link[=create.biplot]{create.biplot()}}. Default \code{5e5}.}

\item{color.palette}{Character, passed through to \code{\link[=create.biplot]{create.biplot()}}.
Default \code{"rainbow"}.}

\item{panel.width, row.height}{Numeric, width and height (inches) of a
single biplot panel, used to size the saved figure. Defaults \code{3} and \code{3}.}

\item{label.width}{Numeric, width (inches) reserved for the row-label
column on the right of the figure. Default \code{1}.}

\item{label.size}{Numeric, font size (points) for the row labels.
Default \code{12}.}

\item{label.angle}{Numeric, rotation angle (degrees) for the row labels.
Default \code{-90} (reads bottom-to-top, matching a facet_grid right strip).}

\item{save}{Logical, if \code{TRUE} (default), saves a JPEG file to
\code{output.dir}. Otherwise the combined plot is only printed.}

\item{title}{Character, used as the JPEG filename stem. Default
\code{"unmix_comparison"}.}

\item{output.dir}{Optional output directory. Default \code{NULL}, in which
case the current working directory is used.}
}
\value{
Invisibly, the combined \code{cowplot} object.
}
\description{
Lays out biplots for two or more pre-unmixed data sets (matrices or data
frames of the same panel) as a grid, one row per data set, so that the
same channel pairs can be compared side-by-side across conditions (for
example, cells vs. beads, or before vs. after a correction step).

Channels are paired up sequentially rather than plotted N x N: for
\code{channels = c("APC", "FITC", "PE", "PerCP", "BV421")} the pairs are
APC x FITC, PE x PerCP, and BV421 x APC (the first channel is repeated
to complete the last pair when an odd number of channels is requested).
Each row uses \code{\link[=create.biplot]{create.biplot()}} for every pair, and the panels are
stitched together with \code{cowplot::plot_grid()}.

If \code{gate.boundary} is supplied and the \code{scatter.param} columns are present
in every data set, \code{\link[=apply.gate]{apply.gate()}} is used to gate every data set to that
boundary before plotting. If any data set is missing a scatter column,
the gate is not applied to any of them (a warning names which data
set(s) were missing it), so that every row in the comparison reflects the
same population.

If \code{data.list} is a named list, the names are used as row labels on the
right of the grid. If unnamed, and \code{data.list} was constructed inline as
\code{list(...)} at the call site, the deparsed expression for each element
(i.e. the variable name passed in) is used instead.
}
\seealso{
\itemize{
\item \code{\link[=create.biplot]{create.biplot()}}
\item \code{\link[=apply.gate]{apply.gate()}}
\item \code{\link[=compare.unmix]{compare.unmix()}}
}
}
