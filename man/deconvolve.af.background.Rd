% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconvolve_af_background.R
\name{deconvolve.af.background}
\alias{deconvolve.af.background}
\title{Deconvolve Autofluorescence Background}
\usage{
deconvolve.af.background(
  raw.data,
  spectra,
  af.basis,
  af.name = "AF",
  target = NULL,
  max.hotspot = 5,
  nonneg = TRUE
)
}
\arguments{
\item{raw.data}{Numeric matrix (events x detectors), raw detector data.}

\item{spectra}{Numeric matrix (fluorophores x detectors), reference spectra.
Any autofluorescence row named by \code{af.name} is removed before fitting, since
the basis supersedes it.}

\item{af.basis}{Numeric matrix (components x detectors) from
\code{get.af.basis()}.}

\item{af.name}{Character or \code{NULL}, the name of an autofluorescence row in
\code{spectra} to drop. Default \code{"AF"}.}

\item{target}{Character or \code{NULL}. When supplied, autofluorescence components
that this fluorophore cannot be separated from are dropped before fitting.
Default \code{NULL}.}

\item{max.hotspot}{Numeric, the hotspot scale above which a component is
considered inseparable from \code{target}. Default \code{5}.}

\item{nonneg}{Logical, whether to clamp fitted autofluorescence coefficients
at zero before subtraction, so background can only be removed and never
added. Default \code{TRUE}.}
}
\value{
A named list:
\describe{
\item{\code{residual}}{\code{raw.data} with the fitted autofluorescence removed.}
\item{\code{background}}{The fitted autofluorescence contribution.}
\item{\code{abundance}}{Fluorophore abundances from the joint fit.}
\item{\code{af.coef}}{Per-event autofluorescence component coefficients, clamped
at zero when \code{nonneg} is \code{TRUE}.}
\item{\code{af.coef.raw}}{The same coefficients before clamping, for use as
regressors.}
\item{\code{hotspot}}{Hotspot matrix of the joint design.}
\item{\code{af.used}, \code{af.dropped}}{Component names kept and dropped.}
}
}
\description{
Removes autofluorescence from raw detector data by fitting it jointly with
the panel rather than subtracting it beforehand. Each event is unmixed
against a design of the autofluorescence basis stacked on the fluorophore
spectra and only the fitted autofluorescence part is subtracted, so a
fluorophore's abundance and the background amount are estimated together
instead of competing for the same signal.

Fluorophores whose spectra lie close to the span of the autofluorescence
basis cannot be separated from it, and projecting them out removes real
signal. The returned \code{hotspot} matrix reports that coupling. When \code{target} is
supplied, components coupling to that fluorophore above \code{max.hotspot} are
dropped from its fit; the leading component is always retained, because every
event carries mean background.
}
