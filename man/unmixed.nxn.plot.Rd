% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_unmixed_nxn.R
\name{unmixed.nxn.plot}
\alias{unmixed.nxn.plot}
\title{n \eqn{\times} n Unmixed Biplot Triangle}
\usage{
unmixed.nxn.plot(
  unmixed.data,
  asp,
  channels = NULL,
  max.points = 50000,
  title = "nxn_biplot",
  biplot.size = 3,
  x.min = -1000,
  y.min = -1000,
  x.width.basis = -1000,
  y.width.basis = -1000,
  use.hex = TRUE,
  hex.bins = 64,
  color.palette = "viridis",
  hist.fill = "steelblue",
  output.dir = "."
)
}
\arguments{
\item{unmixed.data}{Either a character string giving the path to an unmixed
FCS file, or a numeric matrix / data.frame whose columns are fluorophore
channels and whose rows are cells.}

\item{asp}{The AutoSpectral parameter list, created by
\code{\link[=get.autospectral.param]{get.autospectral.param()}}.}

\item{channels}{Optional character vector. Subset of columns to include.
When \code{NULL} (default) all columns are used. Large panels sets (\eqn{n > 20})
will produce very large PDFs; consider subsetting.}

\item{max.points}{Integer. Total number of events retained after a single
random downsample applied before any panel is drawn. Default \code{5e4}.}

\item{title}{Character string used as the PDF filename stem. Default
\code{"nxn_biplot"}.}

\item{biplot.size}{Numeric. Edge length in inches of each individual panel
(both scatter and histogram panels). The saved PDF will be
\code{n * biplot.size} \eqn{\times} \code{n * biplot.size} inches. Default \code{3}.}

\item{x.min}{Numeric. Floor for the auto-scaled x-axis minimum (data
units). The actual minimum used is \code{min(auto, x.min)}. Default \code{-1000}.}

\item{y.min}{Numeric. Floor for the auto-scaled y-axis minimum (data
units). Default \code{-1000}.}

\item{x.width.basis}{Numeric. Width basis for the biexponential x-axis
transform. Default \code{-1000}.}

\item{y.width.basis}{Numeric. Width basis for the biexponential y-axis
transform. Default \code{-1000}.}

\item{use.hex}{Logical. When \code{TRUE} (default), scatter panels are rendered
with \code{\link[ggplot2:geom_hex]{ggplot2::geom_hex()}} for speed. When \code{FALSE}, reverts to
\code{\link[scattermore:geom_scattermore]{scattermore::geom_scattermore()}} + \code{\link[ggplot2:geom_density_2d]{ggplot2::stat_density_2d()}}.}

\item{hex.bins}{Integer. Number of bins for \code{geom_hex} in each dimension.
Ignored when \code{use.hex = FALSE}. Default \code{64}.}

\item{color.palette}{Character string. Viridis palette name (\code{"viridis"},
\code{"magma"}, \code{"inferno"}, \code{"plasma"}, \code{"cividis"}, \code{"rocket"}, \code{"mako"},
\code{"turbo"}) or \code{"rainbow"} to use the package default gradient. Applies to
scatter panels. Default \code{"viridis"}.}

\item{hist.fill}{Character string. Fill color for the diagonal histogram
bars. Default \code{"steelblue"}.}

\item{output.dir}{Character string. Directory for the output PDF. Created
automatically if absent. Default \code{"."}.}
}
\value{
The combined \code{\link[cowplot:plot_grid]{cowplot::plot_grid()}} object is returned invisibly.
The PDF is always written to \code{output.dir}.
}
\description{
Produces a lower-triangular grid of scatter biplots for every pair of
channels in an unmixed dataset, with 1-D histograms on the diagonal, and
saves the result as a single large-format PDF.

For \eqn{n} channels the grid is \eqn{n \times n} panels.  The upper
triangle and all cells above the diagonal are left blank, giving
\eqn{n(n-1)/2} scatter panels and \eqn{n} histogram panels.  Panel
\eqn{(i, j)} (row \eqn{i}, column \eqn{j}) shows column \eqn{j} on the
x-axis and row \eqn{i} on the y-axis, consistent with the convention used
by \code{\link[=unmixed.mxn.plot]{unmixed.mxn.plot()}}: the named fluorophore always appears on x.

Data are downsampled once to \code{max.points} events before any panel is drawn
so all panels share an identical cell population.  Axis minima are
auto-scaled per channel from the 1st percentile of the data, floored at
the user-supplied \code{x.min} / \code{y.min}.
}
\seealso{
\code{\link[=unmixed.mxn.plot]{unmixed.mxn.plot()}}, \code{\link[=create.biplot]{create.biplot()}}
}
