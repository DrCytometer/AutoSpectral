% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contour_polygons_from_grid.R
\name{.contour.polygons.from.grid}
\alias{.contour.polygons.from.grid}
\title{Contour Polygons From a Density Grid}
\usage{
.contour.polygons.from.grid(x, y, z, breaks)
}
\arguments{
\item{x}{Numeric vector of grid coordinates along the matrix rows of \code{z}.}

\item{y}{Numeric vector of grid coordinates along the matrix columns of
\code{z}.}

\item{z}{Numeric matrix of density values, indexed \verb{[x, y]} (rows
correspond to \code{x}, columns to \code{y}) -- the orientation returned by both
\code{fast_kde2d_cpp()} and \code{MASS::kde2d()}. Transposed internally to match
\code{isoband::isobands()}'s \verb{[y, x]} convention.}

\item{breaks}{Numeric vector of band boundaries; consecutive pairs define
each filled band, matching the \code{breaks} argument of
\code{ggplot2::geom_contour_filled()}.}
}
\value{
A data frame with columns \code{x}, \code{y}, \code{subgroup} (isoband's own
per-piece/hole identifier, for the \code{subgroup} aesthetic) and \code{level} (an
ordered factor spanning every band implied by \code{breaks}, low to high, so
colour-to-band mapping stays fixed regardless of which bands are empty in
a given plot). Pass as:
\code{geom_polygon(data = ..., aes(x, y, group = level, subgroup = subgroup, fill = level))}. Returns \code{NULL} if \code{breaks} has fewer than two elements or
every band is empty.
}
\description{
Converts a pre-computed 2D density grid (as returned by
\code{AutoSpectralRcpp::fast_kde2d_cpp()} or \code{MASS::kde2d()}) directly into
filled contour polygons using \code{isoband::isobands()}, ready to hand to
\code{ggplot2::geom_polygon()}. This bypasses \code{ggplot2::stat_contour_filled()},
which would otherwise re-derive the same grid from a long-format melt of
the density matrix before running the identical isobanding step.
}
