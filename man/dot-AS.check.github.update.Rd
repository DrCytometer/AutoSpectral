% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{.AS.check.github.update}
\alias{.AS.check.github.update}
\title{Check GitHub for a newer tagged release of a package}
\usage{
.AS.check.github.update(pkg, repo, installed.version, cache.days = 1)
}
\arguments{
\item{pkg}{Package name, used as the cache subdirectory.}

\item{repo}{GitHub "owner/repo" string.}

\item{installed.version}{A \code{package_version} to compare against.}

\item{cache.days}{Minimum days between live checks. Default 1.}
}
\value{
A \code{package_version} if a newer tag exists, otherwise NULL
(invisibly).
}
\description{
Queries the GitHub tags API for \code{repo}, compares the newest semantic-
version tag against \code{installed.version}, and returns that version if it is
newer. Silently returns NULL on any failure (no network, rate limit,
unparseable response, non-interactive session) since this is a convenience
check and must never interrupt or slow down a script. Results are cached
on disk for \code{cache.days} so a session does not re-query GitHub every time
the package is attached.
}
