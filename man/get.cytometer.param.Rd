% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_cytometer_param.R
\name{get.cytometer.param}
\alias{get.cytometer.param}
\title{Resolve Cytometer Parameters from an FCS File}
\usage{
get.cytometer.param(fcs.path, db.path = NULL, verbose = TRUE)
}
\arguments{
\item{fcs.path}{Character scalar. Path to a single raw \code{.fcs} file
representative of the experiment.}

\item{db.path}{Character scalar or \code{NULL}. Path to \code{cytometer_database.csv}.
When \code{NULL} the function looks in the installed AutoSpectral package;
if absent, detector ordering follows FCS acquisition order.}

\item{verbose}{Logical, default \code{TRUE}. Print a message confirming which
cytometer was identified.}
}
\value{
A named list:
\describe{
\item{\code{cyt.label}}{Human-readable cytometer name.}
\item{\code{scatter.param}}{Character vector of FSC/SSC column names.}
\item{\code{cols.detector}}{Fluorescence detector column names in
excitation/emission order.}
\item{\code{af.channel}}{Primary autofluorescence detector name.}
\item{\code{sat.value}}{Numeric raw saturation ceiling.}
\item{\code{db.col}}{Column name used in \code{cytometer_database.csv}.}
\item{\code{keywords}}{Full keyword list from the FCS TEXT segment.}
}
}
\description{
Reads the FCS keyword metadata from a single representative file and
returns a list of instrument-specific parameters needed to correctly identify
fluorescence detector columns, scatter columns, the primary autofluorescence
channel, the saturation ceiling, and the canonical detector ordering.

Recognition is fully data-driven: no cytometer name needs to be supplied by
the user. The function inspects \verb{$CYT}, \code{CREATOR}, and the set of \verb{$PnN}
parameter names encoded in the FCS TEXT segment.

Confirmed \verb{$CYT} values (from real instrument FCS files):
\itemize{
\item Cytek Aurora / Northern Lights: \code{"Aurora"} (distinguished by UV detectors)
\item Sony ID7000: \code{"ID7000"}
\item BD FACSDiscover A8: \code{"FACSDiscover A8"}
\item BD FACSymphony A5 SE: \code{"FACSymphony Analyzer"}
\item NovoCyte Opteon: \code{"NovoCyte Opteon"}
\item CytoFLEX Mosaic: \code{"CytoFLEX mosaic 88 & CytoFLEX LX"}
\item Attune Xenith: \code{"Attune Xenith"}
}
}
\examples{
\dontrun{
p <- get.cytometer.param( "path/to/control.fcs" )
p$cyt.label      # "Cytek Aurora"
p$af.channel     # "V7-A"
p$cols.detector  # c("UV1-A", "UV2-A", ..., "R8-A")
}

}
