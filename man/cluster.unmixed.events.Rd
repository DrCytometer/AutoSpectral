% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_unmixed_events.R
\name{cluster.unmixed.events}
\alias{cluster.unmixed.events}
\title{Cluster Unmixed Events into Micro-Clusters}
\usage{
cluster.unmixed.events(
  unmixed,
  raw.data,
  asp,
  method = c("som", "stratify", "leiden", "kmeans"),
  cluster.id = NULL,
  n.clusters = 150,
  som.dim = 20,
  k.neighbors = 15,
  resolution = 1,
  unmixed.thresholds = NULL,
  n.levels = 10L,
  stratify.exclude = NULL,
  centroid.fun = c("median", "mean"),
  min.cluster.size = 5,
  cluster.cofactor = 500,
  threads = NULL,
  verbose = TRUE
)
}
\arguments{
\item{unmixed}{Numeric matrix (events x fluorophores), OLS/WLS unmixed
abundance for the sample under investigation. Typically the output of
\code{unmix.ols.fast()} on \code{raw.data} with the current \code{spectra}.}

\item{raw.data}{Numeric matrix (events x detectors), the raw detector-space
data that produced \code{unmixed}. Column order and names must match the
detectors of \code{spectra}.}

\item{asp}{The AutoSpectral parameter list from \code{get.autospectral.param()}.
Used for \code{asp$bird.seed}.}

\item{method}{Character, \code{"som"} (default), \code{"leiden"}, or \code{"kmeans"}.
\code{"som"} uses the package's own batch SOM (\code{get.som.codes()}, the
\code{AutoSpectralRcpp}-accelerated engine already used by \code{get.af.spectra()}
and \code{get.fluor.variants()}, not \code{FlowSOM}/\code{EmbedSOM} directly), with
events assigned to their nearest node via \code{FNN::knnx.index()}.
\code{"leiden"} builds a k-nearest-neighbour graph (\code{FNN::get.knn()}) and
clusters it with \code{igraph::cluster_leiden()} - \code{igraph} is not currently
a package dependency, so this method needs it installed separately for
now. \code{"kmeans"} gives an exact cluster count and is simplest to reason
about, but slower at high event counts and high k.}

\item{cluster.id}{Optional integer vector, length \code{nrow(unmixed)}, a
previously-computed cluster assignment (e.g. \verb{$cluster.id} from an
earlier call). When supplied, clustering is skipped entirely and this
assignment is used directly to compute centroids - intended for reuse
across correction iterations in \code{correct.unmixing.signatures()}, so
cluster identity stays fixed while only \code{unmixed} (and therefore
\code{x.clust}) changes as \code{spectra} is corrected. Default \code{NULL} (cluster
fresh).}

\item{n.clusters}{Target cluster count for \code{method = "kmeans"}. Default
\code{150}. Not used by \code{"som"} (see \code{som.dim}) or \code{"leiden"} (see
\code{resolution}).}

\item{som.dim}{Integer, SOM grid side length for \code{method = "som"}
(\code{som.dim^2} nodes). Default \code{20}.}

\item{k.neighbors}{Integer, neighbours per event for the k-NN graph in
\code{method = "leiden"}. Default \code{15}.}

\item{resolution}{Numeric, resolution parameter passed to
\code{igraph::cluster_leiden()} for \code{method = "leiden"}. Higher values give
more, smaller clusters. Default \code{1}. Unlike \code{n.clusters}/\code{som.dim}, this
does not target an exact cluster count directly - see Exploration
points.}

\item{unmixed.thresholds}{Named numeric vector of per-fluorophore positivity
thresholds in unmixed space. Required for \code{method = "stratify"}, ignored
otherwise.}

\item{n.levels}{Integer, number of abundance bins per fluorophore for
\code{method = "stratify"}. Default \code{10}.}

\item{stratify.exclude}{Optional character vector of fluorophore names that
are not eligible to be an event's dominant fluorophore under
\code{method = "stratify"}. Autofluorescence belongs here: it is present in
every event and would otherwise dominate every assignment. Default \code{NULL}.}

\item{centroid.fun}{Character, \code{"median"} (default) or \code{"mean"}. Function
used to summarise each cluster's abundance and raw-data vectors. Median
is more robust to within-cluster outliers.}

\item{min.cluster.size}{Integer. Clusters with fewer than this many events
are dropped before the regression step, since their centroids are
unstable. Default \code{5}.}

\item{cluster.cofactor}{Numeric, asinh cofactor applied to \code{unmixed} before
clustering only. Abundances span several decades, so linear-scale
distances are dominated by the brightest population; clustering on
\code{asinh(unmixed / cluster.cofactor)} distributes nodes across the whole
dynamic range. Centroids are always computed in linear units. Default
\code{500}.}

\item{threads}{Integer, OpenMP threads for \code{method = "som"}. Default
\code{NULL} (all available cores, matching \code{get.som.codes()}'s own default).
Ignored by other methods.}

\item{verbose}{Logical, controls messaging. Default \code{TRUE}.}
}
\value{
A named list:
\describe{
\item{\code{cluster.id}}{Integer vector, length \code{nrow(unmixed)}, the cluster
assignment for every event (\code{NA} for events in clusters dropped by
\code{min.cluster.size}).}
\item{\code{x.clust}}{Numeric matrix (clusters x fluorophores), the
per-cluster centroid abundance vector. Row names are cluster IDs.}
\item{\code{y.clust}}{Numeric matrix (clusters x detectors), the per-cluster
centroid raw-data vector, matched row-for-row to \code{x.clust}.}
\item{\code{cluster.size}}{Integer vector, length \code{nrow(x.clust)}, the number
of events contributing to each cluster centroid.}
}
}
\description{
Groups events into small, homogeneous micro-clusters based on their
currently-unmixed abundance profile, and computes a per-cluster centroid
abundance vector and matched raw-detector-space centroid vector. This is
Phase 1 of the signature-error-correction workflow: it stabilises
cluster-level autofluorescence/background variation and prevents a few
dominant populations from swamping the downstream regression, while still
sampling the abundance range of every fluorophore.
}
\section{Exploration points}{

\itemize{
\item SOM vs Leiden vs kmeans vs stratify: SOM grid cells are not
guaranteed to be evenly populated, which is especially visible on data
with strong block structure (e.g. concatenated single-stained
controls, where most of the grid may be near-empty for any one
fluorophore's positive population); Leiden should adapt cluster count
to the actual density structure instead of a fixed grid, at the cost
of not targeting an exact cluster count directly; kmeans gives the
most uniform cluster sizes but assumes roughly spherical clusters in
abundance space; stratify guarantees every fluorophore the same
number of populated abundance levels regardless of rarity, at the
cost of needing \code{unmixed.thresholds} up front. Worth comparing all
four on the same real data.
\item \code{resolution} (Leiden) doesn't target an exact \code{n.clusters} the way
\code{som.dim}/\code{n.clusters} do for the other methods. A wrapper that
adjusts \code{resolution} until \code{length(unique(cluster.id))} lands near a
target could be added if a specific count is needed - not implemented
here.
\item The Leiden graph is currently unweighted (plain k-NN edges); an
inverse-distance-weighted graph is a common refinement worth trying.
\item median vs mean centroids: median is the codebase default
elsewhere, but for skewed clusters, mean might track a systematic
error pattern more faithfully. Worth comparing both.
}
}

