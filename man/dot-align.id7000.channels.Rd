% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_id7000_channels.R
\name{.align.id7000.channels}
\alias{.align.id7000.channels}
\title{Align ID7000 Database Channel Names to an FCS File}
\usage{
.align.id7000.channels(x, file.channels, database = NULL)
}
\arguments{
\item{x}{Character vector of ID7000 channel names as written in the
\code{ID7000} column of \code{cytometer_database.csv} (or in the ID7000 spectral
reference library).}

\item{file.channels}{Character vector of channel names found in the acquired
data, or the detector names of a spectra matrix built from it.}

\item{database}{Data frame read from \code{cytometer_database.csv}. Default
\code{NULL}, which reads the copy installed with AutoSpectral.}
}
\value{
Character vector the same length and order as \code{x}.
}
\description{
Internal helper. ID7000 detector names are built from a laser label, "CH", a
detector number and a suffix (e.g. "320CH12-A"). Some instruments write a
different laser label into the FCS file (e.g. "DUVCH12-A"). The
\code{cytometer_database.csv} therefore holds a second name for each ID7000
detector in the \code{ID7000_alt} column.

For each database detector, the alternate name is used only when the primary
name (\code{ID7000} column) is not among \code{file.channels} and the alternate name
is. Otherwise the primary name is kept. The choice is made one detector at a
time, so an instrument that relabels only some lasers is handled, and an
alternate name that never occurs in the data has no effect.

Names in \code{x} that are not ID7000 database names (scatter, time, blanks,
\code{NA}) are returned unchanged.
}
\keyword{internal}
