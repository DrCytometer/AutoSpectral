% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_retained_channels.R
\name{.select.retained.channels}
\alias{.select.retained.channels}
\title{Select Retained Channels}
\usage{
.select.retained.channels(
  original.param,
  spectral.channel,
  asp,
  include.raw = FALSE,
  include.imaging = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{original.param}{Character vector of the parameter names (\verb{$PnN}) of the
raw FCS file, in file order.}

\item{spectral.channel}{Character vector of the detector channels used for
unmixing (the column names of the spectra matrix).}

\item{asp}{The AutoSpectral parameter list. Prepare using
\code{get.autospectral.param}.}

\item{include.raw}{Logical, whether to retain the raw detector data. Default
is \code{FALSE}.}

\item{include.imaging}{Logical, whether to retain imaging parameters. Only
applies to the BD FACSDiscover family, where \code{FALSE} restricts the retained
parameters to time and scatter. Default is \code{TRUE}.}

\item{verbose}{Logical, whether to report the parameters that are not carried
through. Default is \code{TRUE}.}
}
\value{
Character vector of the parameter names to carry through, in the
order they are to be written.
}
\description{
Internal helper for \code{unmix.fcs()}. Selects the parameters of a raw FCS file
that are carried through unchanged into the unmixed file, alongside the newly
unmixed fluorophore parameters.

Some instruments (e.g. the BD FACSDiscover family and the FACSymphony A5 SE)
write "raw" FCS files that also hold the instrument's own unmixed
fluorophore parameters. These are never carried through: they would
duplicate, and could be mistaken for, the AutoSpectral output. A parameter
is retained only if it matches one of the named parameter families in
\code{asp$non.spectral.channel} (time, scatter, imaging and other acquisition
parameters). Bare suffix entries in that vector (\code{-H}, \code{-W}, \code{-T}) exist to
keep detector companions out of the spectral channel set and are not used to
retain anything, since fluorophore names such as \code{APC-H7} would otherwise
match them.

Raw detector data are written only when \code{include.raw = TRUE}. For each
detector used in unmixing this is the \code{-A} (or \code{-H}) parameter that was
unmixed, plus its \code{-T} companion where the file has one. The \code{-H} and \code{-W}
companions of the detectors are never written.
}
\keyword{internal}
