% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unmix_gls.R
\name{build.variant.basis}
\alias{build.variant.basis}
\title{Build Variant Covariance Basis}
\usage{
build.variant.basis(
  spectra.variants,
  rank = 6L,
  var.explained = 0.99,
  min.lambda = 1e-04,
  pooled.fallback = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{spectra.variants}{Either the full list returned by
\code{get.spectral.variants()} (containing \verb{$delta.list}), or a named list of
delta matrices (variants x detectors).}

\item{rank}{Integer, maximum number of components retained per
fluorophore. Default \code{2}.}

\item{var.explained}{Numeric in (0, 1]. Components are dropped once this
fraction of the delta variance is captured. Default \code{0.99}.}

\item{min.lambda}{Numeric, eigenvalues below this fraction of the leading
eigenvalue are dropped. Default \code{1e-4}.}

\item{pooled.fallback}{Logical, if \code{TRUE}, uses other fluorophores' variability
to infer likely variability for a fluorophore where variants where not
sucessfully measured.}

\item{verbose}{Logical, whether to output messages to the console.}
}
\value{
A named list, one entry per fluorophore with at least two
variants, each containing \code{basis} (components x detectors), \code{lambda}
(numeric, one per component), and \code{mean.delta} (numeric, length
detectors). Fluorophores with insufficient variants are omitted.
}
\description{
Converts the per-fluorophore variant deltas from \code{get.spectral.variants()}
into a low-rank covariance basis for \code{unmix.gls()}. Each fluorophore's
contribution to the detector-space covariance is
\eqn{x_f^2 \sum_j \lambda_{fj} b_{fj} b_{fj}^\top}.

The \emph{mean} delta is returned separately rather than folded into the
covariance: a systematic offset from the reference spectrum is a
correction to \code{spectra}, not a source of variance, and treating it as
variance both inflates the noise model and leaves the bias uncorrected.
}
