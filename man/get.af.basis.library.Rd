% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_af_basis_library.R
\name{get.af.basis.library}
\alias{get.af.basis.library}
\title{Build A Continuous Autofluorescence Basis From The Discrete Library}
\usage{
get.af.basis.library(
  af.spectra,
  spectra,
  n.components = NULL,
  var.explained = 0.99
)
}
\arguments{
\item{af.spectra}{Autofluorescence spectra, variants in rows and detectors
in columns. Prepare using \code{get.af.spectra}.}

\item{spectra}{Fluorophore spectral signatures, fluorophores in rows and
detectors in columns.}

\item{n.components}{Integer, number of basis directions to retain. Default
\code{NULL}, in which case the count is chosen from \code{var.explained}.}

\item{var.explained}{Numeric in (0, 1], default \code{0.99}. Fraction of the
out-of-span variance of the AF library the retained basis must capture.
Ignored when \code{n.components} is supplied.}
}
\value{
A list with \code{basis} (detectors x components), \code{sigma} (singular
values of the projected library), \code{directions} (the orthonormal out-of-span
directions, detectors x components), \code{var.explained} (cumulative fraction
captured) and \code{n.components}.
}
\description{
Constructs a low-dimensional basis for autofluorescence that replaces the
discrete AF library in the unmixing solve. The basis is derived from the
part of the AF library that the fluorophore panel cannot explain, because
only that component is identifiable and only that component reaches the
fluorophore abundances. Ranking by out-of-span energy rather than total
energy means the leading directions are the ones that actually matter, and
it makes the resulting normal equations diagonal, so the per-cell solve is a
single projection with no matrix inverse.
}
