% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_spectra_standard_workflow.R
\name{spectra.standard.workflow.plot}
\alias{spectra.standard.workflow.plot}
\title{Plot Standard (Manual-Gating) Spectra Extraction Workflow}
\usage{
spectra.standard.workflow.plot(
  control.dir,
  control.def.file,
  asp,
  fluorophores = NULL,
  octagon.width.factor = 3,
  n.bright.events = 2000L,
  negative.quantile = 0.25,
  negative.quantile.min = 0.01,
  x.min.quantile = 0.005,
  singlet.quantiles = c(0.85, 0.975),
  gate.color = "darkgoldenrod1",
  density.palette = "rainbow",
  selection.fill.color = "steelblue",
  selection.line.color = "black",
  negative.bracket.color = "#377EB8",
  positive.bracket.color = "#E41A1C",
  negative.point.color = "black",
  event.point.size = NULL,
  n.highlight = 200L,
  clean.positive.color = "red",
  clean.positive.point.size = NULL,
  cells.trace.color = "#D95F02",
  beads.trace.color = "#377EB8",
  af.trace.color = "grey40",
  max.points = 50000,
  panel.width = 4,
  panel.height = 4,
  composite.width = NULL,
  composite.height = NULL,
  output.dir = NULL,
  save = TRUE,
  file.type = "jpg",
  verbose = TRUE
)
}
\arguments{
\item{control.dir}{Character. Path to the directory containing the
single-stained control FCS files.}

\item{control.def.file}{Character. Path to (or filename of) the control
definition CSV -- for the plotted fluorophore itself, only the
\code{fluorophore} / \code{filename} columns are used, since this workflow always
derives an internal negative from the same file. If the file has a
\code{control.type} column (as used by \code{\link[=define.flow.control]{define.flow.control()}} /
\code{\link[=check.control.file]{check.control.file()}}), a row with \code{control.type == "beads"} sharing a
fluorophore name with a "cells" row is treated as a paired bead control:
its \code{filename} is the positive bead sample, and its \code{universal.negative}
must point at the matching unstained bead file. When present, this pair
supplies the "\if{html}{\out{<Fluorophore>}} (Beads)" trace in panel D instead of the
static spectral reference library.}

\item{asp}{The AutoSpectral parameter list from \code{get.autospectral.param()}.}

\item{fluorophores}{Character vector of fluorophore name(s) to illustrate.
Default \code{NULL} illustrates the first fluorophore found.}

\item{octagon.width.factor}{Numeric, default \code{3}. Controls how large the
octagon gate is drawn, as a multiple of the local median-absolute-
deviation spread of events around the density peak.}

\item{n.bright.events}{Integer, default \code{2000}. Number of top-expressing
events on the peak channel (within the octagon gate) selected as the
"positive" population.}

\item{negative.quantile}{Numeric in \verb{(0, 1)}, default \code{0.25}. Upper
quantile bound (on the peak channel, within octagon-gated events) of the
internal "negative" population.}

\item{negative.quantile.min}{Numeric in \verb{[0, negative.quantile)}, default
\code{0.01}. Lower quantile bound of the internal "negative" population --
the negative gate spans events between the \code{negative.quantile.min} and
\code{negative.quantile} quantiles of the peak channel, excluding the extreme
dim tail (typically debris) below \code{negative.quantile.min}.}

\item{x.min.quantile}{Numeric in \verb{[0, 1)}, default \code{0.005}. Quantile of the
peak-channel data used as the x-axis lower limit in panel B. Flow data
can have a long, sparse negative tail, so this is set independently of
\code{negative.quantile.min} to keep the histogram readable.}

\item{singlet.quantiles}{Numeric, default \code{c(0.85, 0.975)}. Quantile
thresholds used only when cleaning a paired bead control (see
\code{control.def.file}), matching \code{\link[=get.spectra.automated]{get.spectra.automated()}}.}

\item{gate.color}{Colour of the octagon gate boundary in panel A, and of
the gate box drawn around the positive fraction in panel C. Default
\code{"darkgoldenrod1"} (matching \code{do.gate()}'s default).}

\item{density.palette}{Fill palette for the pseudocolour density in panel
A: one of the viridis options (\code{"plasma"}, \code{"viridis"}, etc.) or any
other value to use \code{asp$density.palette.base.color}. Default \code{"rainbow"}
(matching \code{create.biplot()}'s default).}

\item{selection.fill.color, selection.line.color}{Fill and line colours for
the KDE histogram in panel B. Defaults \code{"steelblue"} / \code{"black"}.}

\item{negative.bracket.color, positive.bracket.color}{Colours for the
negative- and positive-selection brackets in panel B. Defaults
\code{"#377EB8"} (blue) / \code{"#E41A1C"} (red).}

\item{negative.point.color}{Colour for the negative-fraction events in
panel C, which are plotted as plain points with no colour mapping
(matching the "Unstained" convention in
\code{\link[=spectra.automated.steps.plot]{spectra.automated.steps.plot()}}'s panel D). Default \code{"black"}. Only the
positive-fraction events are coloured by cosine similarity.}

\item{event.point.size}{Numeric or \code{NULL} (default). Point size for the
positive-fraction events in panel C. If \code{NULL}, defaults to
\code{asp$figure.gate.point.size * 1.3}.}

\item{n.highlight}{Integer, default \code{200}. Number of positive-fraction
events (smallest cosine similarity to the negative fraction, i.e. least
AF-like) highlighted in red in panel A.}

\item{clean.positive.color}{Colour for the panel A highlight. Default
\code{"red"}.}

\item{clean.positive.point.size}{Numeric or \code{NULL} (default). Point size
for the panel A highlight. If \code{NULL}, defaults to
\code{asp$figure.gate.point.size * 1.5}.}

\item{cells.trace.color, beads.trace.color, af.trace.color}{Colours for the
three traces in panel D: the population-level background-subtracted
profile ("Cells"), the reference profile ("Beads"), and the negative-
fraction trace. Defaults \code{"#D95F02"} / \code{"#377EB8"} / \code{"grey40"}.}

\item{max.points}{Integer. Maximum events plotted per panel (randomly
downsampled beyond this for speed). Default \code{5e4}.}

\item{panel.width, panel.height}{Numeric. Width/height (inches) used per
sub-panel when sizing the saved composite figure. Defaults \code{4} and \code{4}.}

\item{composite.width, composite.height}{Numeric or \code{NULL} (default).
Override the overall saved figure dimensions (inches); if \code{NULL}, these
are computed from \code{panel.width} / \code{panel.height}.}

\item{output.dir}{Character or \code{NULL} (default). Directory to save the
composite figure(s). Defaults to the current working directory.}

\item{save}{Logical, default \code{TRUE}. Whether to save the composite figure
for each fluorophore to \code{output.dir}.}

\item{file.type}{Character string, one of \code{"jpg"} (default), \code{"tiff"},
\code{"png"}, or \code{"pdf"}.}

\item{verbose}{Logical, default \code{TRUE}. Print progress messages.}
}
\value{
Invisibly, a named list (one entry per fluorophore) each
containing the individual panel ggplot objects, the assembled
\code{composite} cowplot object, and the peak / peak-AF channels used.
}
\description{
Builds a manuscript-ready, multi-panel figure illustrating a "standard"
single-stained-control workflow of the kind used in vendor software (e.g.
SpectroFlo): an octagon gate on FSC-A vs SSC-A positioned on the dominant
population, selection of the brightest events on the fluorophore's peak
channel as the positive population and the dimmest fraction as an internal
negative, biplots of both fractions (negative in black, positive coloured
by cosine similarity), and a population-
level background subtraction. Intended as a direct comparison against
\code{\link[=spectra.automated.steps.plot]{spectra.automated.steps.plot()}} for the same fluorophore(s). Uses the same
building blocks as the rest of AutoSpectral (\code{\link[=create.biplot]{create.biplot()}}-style
pseudocolour density, \code{\link[=spectral.trace]{spectral.trace()}}) so panel styling matches the
package's other figures.

Requires \code{plot_spectra_automated_steps.R} to be loaded in the same package
namespace (several private helpers are shared).
}
\seealso{
\code{\link[=spectra.automated.steps.plot]{spectra.automated.steps.plot()}}, \code{\link[=do.gate]{do.gate()}}, \code{\link[=create.biplot]{create.biplot()}},
\code{\link[=spectral.trace]{spectral.trace()}}
}
