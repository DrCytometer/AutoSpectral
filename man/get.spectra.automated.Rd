% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_spectra_automated.R
\name{get.spectra.automated}
\alias{get.spectra.automated}
\title{Get Fluorophore Spectra - Automated Workflow}
\usage{
get.spectra.automated(
  control.dir,
  control.def.file,
  asp,
  n.candidates = 1000L,
  n.spectral = 200L,
  k.neighbors = 2L,
  cosine.threshold = 0.9,
  peak.signal.threshold = 0.5,
  top.expressing.override = NULL,
  figures = TRUE,
  plot.cosine.filter = TRUE,
  plot.scatter.match = TRUE,
  verbose = TRUE,
  print.timings = FALSE
)
}
\arguments{
\item{control.dir}{Character. Path to the directory containing the
single-stained control FCS files.}

\item{control.def.file}{Character. Path to (or filename of) the control
definition CSV. Must already exist and pass \code{check.control.file()}.}

\item{asp}{The AutoSpectral parameter list from \code{get.autospectral.param()}.}

\item{n.candidates}{Integer, default \code{1000}. Number of top-expressing
candidate events selected per fluorophore before cosine-similarity
filtering. Ignored in internal-negative mode, where the top 5\%\% of
events by peak channel are used directly.}

\item{n.spectral}{Integer, default \code{200}. Number of spectral events
retained after filtering for low AF cosine similarity.}

\item{k.neighbors}{Integer, default \code{2}. Number of nearest neighbours in
scatter space used for per-event AF subtraction.}

\item{cosine.threshold}{Numeric, default \code{0.9}. Minimum cosine similarity
against the spectral reference library to accept the automated spectrum;
values below this threshold trigger RLM refinement.}

\item{peak.signal.threshold}{Numeric, default \code{0.5}. Minimum normalised
signal in the expected peak detector to accept the automated spectrum;
values below this threshold trigger RLM refinement.}

\item{top.expressing.override}{Named numeric vector or \code{NULL} (default).
Override the event count for specific samples. Names should match the FCS
filename (without extension) or the fluorophore name from the control file.}

\item{figures}{Logical, default \code{TRUE}. Produce spectral trace, heatmap,
cosine-similarity, and reference-library QC plots.}

\item{plot.cosine.filter}{Logical, default \code{TRUE}. When \code{figures = TRUE},
produce a multi-panel PDF showing the per-fluorophore cosine-similarity
filter traces (binned by cosine similarity to AF, coloured from least
to most AF-like). Set to \code{FALSE} to skip.}

\item{plot.scatter.match}{Logical, default \code{TRUE}. When \code{figures = TRUE},
produce a multi-panel PDF showing the KNN scatter-matched AF subtraction
for each fluorophore (unstained background, selected spectral events, and
their matched AF events). Set to \code{FALSE} to skip.}

\item{verbose}{Logical, default \code{TRUE}. Print progress messages.}

\item{print.timings}{Logical, default \code{FALSE}. When \code{TRUE}, prints a
\code{tictoc} timing log to the console at the end of the call, showing
elapsed time for each major processing stage.}
}
\value{
A numeric matrix with fluorophores in rows and spectral detector
channels in columns, values normalised to \verb{[0, 1]} (L-infinity norm,
peak = 1). Compatible with all downstream AutoSpectral functions.
}
\description{
Single-function replacement for the three-step
\code{define.flow.control} -> \code{clean.controls} -> \code{get.fluorophore.spectra} pipeline.
Extracts normalised \verb{[0, 1]} fluorophore reference spectra from a directory of
single-stained control FCS files without requiring scatter gating or interactive
input.

The algorithm is based on the \code{spectracle} approach, adapted to use
AutoSpectral's internal FCS reader, matrix-based data layout, and the
control-file metadata already required by the rest of the AutoSpectral
pipeline:
\enumerate{
\item Reads all single-stained controls with \code{readFCS()}.
\item Removes saturating events.
\item For each fluorophore, determines the AF reference from the paired
universal-negative file specified in \code{universal.negative} column of the
control table, or - if that column is empty - from the lower 25\% of
the control file's own events ranked by the expected peak channel
(internal-negative mode).
\item Performs projection-based AF orthogonalisation to identify the empirical
peak detector; selects top-expressing events filtered by lowest cosine
similarity to the AF vector; performs KNN scatter-matched AF subtraction.
\item Applies QC: if the normalised signal in the expected peak detector is
below \code{peak.signal.threshold} \strong{or} the cosine similarity against the
spectral reference library is below \code{cosine.threshold}, the spectrum is
refined using the RLM approach along the expected peak channel.
\item Returns the spectra matrix in AutoSpectral format (fluorophores x detectors).
}

The legacy workflow (\code{define.flow.control} + \code{clean.controls} +
\code{get.fluorophore.spectra}) remains available and is unchanged.
}
\seealso{
\code{\link[=get.fluorophore.spectra]{get.fluorophore.spectra()}} for the legacy workflow.
\code{\link[=get.cytometer.param]{get.cytometer.param()}} for the cytometer auto-detection step.
\code{\link[=spectral.reference.plot]{spectral.reference.plot()}} for the QC report produced when \code{figures = TRUE}.
}
