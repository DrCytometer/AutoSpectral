% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_unmixed_mxn.R
\name{unmixed.mxn.plot}
\alias{unmixed.mxn.plot}
\title{m \eqn{\times} n Unmixed Biplot Grid}
\usage{
unmixed.mxn.plot(
  unmixed.data,
  fluorophore,
  asp,
  channels = NULL,
  max.points = 50000,
  title = "mxn_biplot",
  biplot.size = 3,
  n.col = NULL,
  x.min = -1000,
  y.min = -1000,
  x.width.basis = -1000,
  y.width.basis = -1000,
  use.hex = TRUE,
  hex.bins = 64,
  color.palette = "viridis",
  output.dir = "."
)
}
\arguments{
\item{unmixed.data}{Either a character string giving the path to an unmixed
FCS file, or a numeric matrix / data.frame whose columns are fluorophore
channels and whose rows are cells.}

\item{fluorophore}{Character vector of length \eqn{m}. Names of the target
fluorophores to place on the x-axis (one row of panels per entry). Every
element must be present in \code{colnames(unmixed.data)} (or the FCS columns).}

\item{asp}{The AutoSpectral parameter list, created by
\code{\link[=get.autospectral.param]{get.autospectral.param()}}.}

\item{channels}{Optional character vector. Subset of channels to use as
y-axis columns. When \code{NULL} (default) all columns except those in
\code{fluorophore} are used. Must be present in the data if supplied.}

\item{max.points}{Integer. Total number of events to retain after a single
random downsample applied before drawing any panel. Default \code{5e4}.}

\item{title}{Character string used as the PDF filename stem and as the
overall plot title. Default \code{"mxn_biplot"}.}

\item{biplot.size}{Numeric. Edge length in inches of each individual panel.
Default \code{3}.}

\item{n.col}{Integer. Number of panel columns per page. When \code{NULL}
(default) this is set to the number of y-axis channels, giving one row of
panels per target fluorophore on a single page.}

\item{x.min}{Numeric. Floor for the auto-scaled x-axis minimum (data
units). The actual minimum used is \code{min(auto, x.min)}. Default \code{-1000}.}

\item{y.min}{Numeric. Floor for the auto-scaled y-axis minimum (data
units). Default \code{-1000}.}

\item{x.width.basis}{Numeric. Width basis for the biexponential x-axis
transform (passed to \code{\link[flowWorkspace:flowjo_biexp]{flowWorkspace::flowjo_biexp()}}). Default \code{-1000}.}

\item{y.width.basis}{Numeric. Width basis for the biexponential y-axis
transform. Default \code{-1000}.}

\item{use.hex}{Logical. When \code{TRUE} (default), panels are rendered with
\code{\link[ggplot2:geom_hex]{ggplot2::geom_hex()}} for speed. When \code{FALSE}, reverts to
\code{\link[scattermore:geom_scattermore]{scattermore::geom_scattermore()}} + \code{\link[ggplot2:geom_density_2d]{ggplot2::stat_density_2d()}}.}

\item{hex.bins}{Integer. Number of bins for \code{geom_hex} in each dimension.
Ignored when \code{use.hex = FALSE}. Default \code{64}.}

\item{color.palette}{Character string. Viridis palette name (\code{"viridis"},
\code{"magma"}, \code{"inferno"}, \code{"plasma"}, \code{"cividis"}, \code{"rocket"}, \code{"mako"},
\code{"turbo"}) or \code{"rainbow"} to use the package default gradient. Default
\code{"viridis"}.}

\item{output.dir}{Character string. Directory for the output PDF. Created
automatically if absent. Default \code{"."}.}
}
\value{
The combined \code{\link[cowplot:plot_grid]{cowplot::plot_grid()}} object is returned invisibly.
The PDF is always written to \code{output.dir}.
}
\description{
Produces a grid of scatter biplots comparing a set of m target fluorophores
(x-axis) against every other channel in the dataset (y-axis), and saves the
result as a multi-panel PDF.  Each target fluorophore defines one row of
panels; the columns correspond to all other selected channels.

Data are downsampled once to \code{max.points} events before any panel is drawn,
so all panels share the same cell population.  Axis minima are auto-scaled
per channel using the 1st percentile of the data, floored at the
user-supplied \code{x.min} / \code{y.min}.

Panels can use either \code{geom_hex} (default, fastest) or the standard
\code{geom_scattermore} + \code{stat_density_2d} combination from \code{\link[=create.biplot]{create.biplot()}}.
}
\seealso{
\code{\link[=unmixed.nxn.plot]{unmixed.nxn.plot()}}, \code{\link[=create.biplot]{create.biplot()}}
}
