% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unmix_af_fwl.R
\name{unmix.af.fwl}
\alias{unmix.af.fwl}
\title{Unmix With Per-Cell Autofluorescence By Frisch-Waugh}
\usage{
unmix.af.fwl(
  raw.data,
  spectra,
  af.spectra,
  af.index,
  unmixed.no.af = NULL,
  return.fitted.af = FALSE,
  denominator.floor = 0,
  chunk.size = 100000L
)
}
\arguments{
\item{raw.data}{Expression data from raw FCS files. Cells in rows and
detectors in columns. Columns must match the columns in \code{spectra}.}

\item{spectra}{Spectral signatures of fluorophores, with fluorophores in
rows and detectors in columns.}

\item{af.spectra}{Spectral signatures of autofluorescences, with variants in
rows and detectors in columns. Prepare using \code{get.af.spectra}.}

\item{af.index}{Integer vector, one entry per cell, giving the row of
\code{af.spectra} assigned to that cell.}

\item{unmixed.no.af}{Optional numeric matrix (cells x fluorophores) holding
the AF-free unmixing \code{raw.data \%*\% t(U)}. Supply it when the caller has
already computed it to avoid repeating the largest matrix product. Default
\code{NULL}, in which case it is computed here.}

\item{return.fitted.af}{Logical, default \code{FALSE}. Whether to also return the
fitted autofluorescence in detector space, \code{k * af.spectra[af.index, ]}.}

\item{denominator.floor}{Numeric, default \code{0}. When positive, floors each
AF candidate's out-of-span self-dot at this fraction of the largest. An AF
variant lying almost inside the fluorophore span has a vanishing out-of-span
direction and an unidentifiable abundance; flooring caps the amplification.
\code{0} reproduces the unregularised least-squares solution exactly.}

\item{chunk.size}{Integer, default \code{1e5}. Number of cells processed per
block, bounding the size of the intermediate detector-wide gather.}
}
\value{
A list with elements \code{fluorophores} (cells x fluorophores), \code{af}
(numeric vector of per-cell AF abundance) and, when requested,
\code{fitted.af} (cells x detectors).
}
\description{
Solves the joint least-squares problem \code{y = t(spectra) \%*\% f + k * af} for
every cell at once, where each cell may use a different autofluorescence
spectrum. The Frisch-Waugh-Lovell decomposition splits the joint solve into
two precomputable library projections plus one pass over the data, so no
per-cell or per-group design matrix is ever formed. Results are identical to
solving each AF group separately with \code{unmix.ols.fast()} on the stacked
spectra, to floating-point precision.
}
