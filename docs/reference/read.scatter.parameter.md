# Read Scatter Parameters

This function reads scatter parameters from a specified file or uses
default scatter parameters if the file is not available.

## Usage

``` r
read.scatter.parameter(asp)
```

## Arguments

- asp:

  The AutoSpectral parameter list. Prepare using
  `get.autospectral.param`

## Value

A vector of scatter parameters.
